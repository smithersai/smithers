Execute the following command and capture its output and exit code:

Command: {props.command}
Working directory: {props.directory}

Run the command, then report the exact exit code, stdout, and stderr.
If the command times out, report it as a failure with a timeout note.
