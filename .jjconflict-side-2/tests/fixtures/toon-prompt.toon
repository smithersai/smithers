imports:
  agents[1]{from,use}:
    ./toon-agent.ts,echo
name: toon-prompt
input:
  name: string
steps[1]:
  - id: greet
    agent: echo
    prompt: "Hello {input.name}\n"
    output:
      message: string
