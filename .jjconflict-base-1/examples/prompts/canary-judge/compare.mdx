export default function Prompt(props) {
  const { children, components, ...data } = props ?? {};
  return (
    <>
      {"Example prompt: canary judge compare"}
      {"\n\n"}
      {JSON.stringify(data, null, 2)}
    </>
  );
}
