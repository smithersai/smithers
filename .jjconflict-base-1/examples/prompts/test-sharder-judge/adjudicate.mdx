export default function Prompt(props) {
  const { children, components, ...data } = props ?? {};
  return (
    <>
      {"Example prompt: test sharder judge adjudicate"}
      {"\n\n"}
      {JSON.stringify(data, null, 2)}
    </>
  );
}
