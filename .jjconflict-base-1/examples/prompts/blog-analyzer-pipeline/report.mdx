export default function Prompt(props) {
  const { children, components, ...data } = props ?? {};
  return (
    <>
      {"Example prompt: blog analyzer pipeline report"}
      {"\n\n"}
      {JSON.stringify(data, null, 2)}
    </>
  );
}
