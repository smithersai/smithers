name: toon-retry
input:
  name: string
steps[1]:
  - id: flaky
    run: "if (attempt < 3) {\n  throw new Error(\"flaky\");\n}\nreturn { ok: true };\n"
    output:
      ok: boolean
    retry:
      maxAttempts: 3
      backoff: fixed
      initialDelay: 30ms
