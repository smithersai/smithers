apiVersion: v1
kind: Service
metadata:
  name: smithers-gateway
  namespace: smithers-system
spec:
  type: LoadBalancer
  selector:
    app: smithers-orchestrator
  ports:
    - name: http
      port: 80
      targetPort: 3000
