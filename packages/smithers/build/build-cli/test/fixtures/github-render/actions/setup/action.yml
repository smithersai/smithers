# Generated by smithers-build from //.github:github. Do not edit; run: smithers-build //.github:github --write
name: setup
description: Install the workspace toolchain and connect the smithers remote cache.
inputs:
  cache-url:
    description: Exported to the job environment as SMITHERS_CACHE_URL.
    required: false
    default: ""
  cache-token:
    description: Exported to the job environment as SMITHERS_CACHE_TOKEN.
    required: false
    default: ""
runs:
  using: composite
  steps:
    - uses: actions/setup-node@v4
      with:
        "node-version-file": "package.json"
    - uses: actions/cache@v4
      with:
        "path": "~/.cache/yarn"
        "key": "yarn-store-${{ hashFiles('yarn.lock') }}"
        "restore-keys": "yarn-store-"
    - run: "yarn install --frozen-lockfile"
      shell: bash
    - run: |
        if [ -n "$SMTHRS_INPUT_CACHE_URL" ]; then
          delimiter="smthrs_$(openssl rand -hex 16 2>/dev/null || od -An -tx1 -N16 /dev/urandom | tr -d ' \n')"
          case "$SMTHRS_INPUT_CACHE_URL" in *"$delimiter"*) exit 1 ;; esac
          printf '%s<<%s\n' SMITHERS_CACHE_URL "$delimiter" >> "$GITHUB_ENV"
          printf '%s\n' "$SMTHRS_INPUT_CACHE_URL" >> "$GITHUB_ENV"
          printf '%s\n' "$delimiter" >> "$GITHUB_ENV"
          case "$SMTHRS_INPUT_CACHE_TOKEN" in *"$delimiter"*) exit 1 ;; esac
          printf '%s<<%s\n' SMITHERS_CACHE_TOKEN "$delimiter" >> "$GITHUB_ENV"
          printf '%s\n' "$SMTHRS_INPUT_CACHE_TOKEN" >> "$GITHUB_ENV"
          printf '%s\n' "$delimiter" >> "$GITHUB_ENV"
        fi
      shell: bash
      env:
        SMTHRS_INPUT_CACHE_URL: ${{ inputs.cache-url }}
        SMTHRS_INPUT_CACHE_TOKEN: ${{ inputs.cache-token }}
