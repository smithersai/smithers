# Generated by smithers-build from //.github:github. Do not edit; run: smithers-build //.github:github --write
name: "danger"
on:
  pull_request:
jobs:
  github-danger:
    runs-on: "ubuntu-latest"
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
        with:
          "cache-url": "${{ secrets.SMITHERS_CACHE_URL }}"
          "cache-token": "${{ secrets.SMITHERS_CACHE_TOKEN }}"
      - run: "yarn exec smithers-build '//.github:danger'"
