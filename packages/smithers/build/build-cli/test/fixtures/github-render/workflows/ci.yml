# Generated by smithers-build from //.github:github. Do not edit; run: smithers-build //.github:github --write
name: "ci"
on:
  pull_request:
  push:
    branches:
      - "main"
  workflow_dispatch:
concurrency:
  group: "ci-${{ github.ref }}"
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}
jobs:
  prePush:
    runs-on: "ubuntu-latest"
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
        with:
          "cache-url": "${{ secrets.SMITHERS_CACHE_URL }}"
          "cache-token": "${{ secrets.SMITHERS_CACHE_TOKEN }}"
      - run: "yarn exec smithers-build '//:prePush'"
  src-deadCode:
    runs-on: "ubuntu-latest"
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
        with:
          "cache-url": "${{ secrets.SMITHERS_CACHE_URL }}"
          "cache-token": "${{ secrets.SMITHERS_CACHE_TOKEN }}"
      - run: "yarn exec smithers-build '//src:deadCode'"
  src-unreachableCode:
    runs-on: "ubuntu-latest"
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
        with:
          "cache-url": "${{ secrets.SMITHERS_CACHE_URL }}"
          "cache-token": "${{ secrets.SMITHERS_CACHE_TOKEN }}"
      - run: "yarn exec smithers-build '//src:unreachableCode'"
  src-routesGen:
    runs-on: "ubuntu-latest"
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
        with:
          "cache-url": "${{ secrets.SMITHERS_CACHE_URL }}"
          "cache-token": "${{ secrets.SMITHERS_CACHE_TOKEN }}"
      - run: "yarn exec smithers-build '//src:routesGen'"
  playwright-smoke:
    runs-on: "ubuntu-latest"
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
        with:
          "cache-url": "${{ secrets.SMITHERS_CACHE_URL }}"
          "cache-token": "${{ secrets.SMITHERS_CACHE_TOKEN }}"
      - run: "yarn exec smithers-build '//playwright:smoke'"
