/**
 * Shared default approval policy for command-line entrypoints.
 *
 * @since 1.0.0
 */
/**
 * Default scope for both CLI approval spellings. MCP hosts choose their own policy.
 * @category constants
 * @since 1.0.0
 */
export const defaultApprovalScope = "run" as const
