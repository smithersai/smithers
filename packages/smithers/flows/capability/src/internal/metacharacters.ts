/**
 * The glob grammar's metacharacters.
 *
 * @since 0.1.0
 */

/**
 * The glob metacharacters. Every other code unit in a resource is literal.
 *
 * @since 0.1.0
 * @private
 */
export const metacharacters = ["*", "?"] as const
