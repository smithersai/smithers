---
revision: example-1
gates: []
---

# Gates

No gate is configured, so every flow runs without routine approval. Add an entry
to `gates` to attach approval, review, budget, concurrency, window or condition
backpressure at a boundary.
