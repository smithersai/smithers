/**
 * The three layer files the router requires at an app root, each a stub
 * export, for a throwaway tree whose subject is something else.
 */
export const layers = {
  "AGENT.ts": "export const Agent = {}\n",
  "SANDBOX.ts": "export const Sandbox = {}\n",
  "TOOLS.ts": "export const Tools = {}\n"
}
