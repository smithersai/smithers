---
title: "API reference"
description: "Every public export of @smthrs/opencode: the server assembly and bind rule, the claim on the served directory, the routes, the event hub, the store, the projection, the health color, the turn composition, the driver seam, the engine driver and the scripted driver, identifiers, CORS, and basic authentication."
---

The package declares `effect`, `@effect/platform-node` and
`@effect/sql-sqlite-node` as exact `4.0.0-rc.115` peers. Use the same Effect
version in the host.

The root entry point exports one namespace per module, and every module is
also importable from `@smthrs/opencode/<Module>`.

## `Serve`

| Export            | Signature                                                                                                | Meaning                                                                                                                         |
| ----------------- | -------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| `Bind`            | `{ hostname, port, listen, cors, credentials }`                                                          | What the verb was asked to bind.                                                                                                |
| `defaultBind`     | `Bind`                                                                                                   | Loopback on 4096, the port the hosted app expects.                                                                              |
| `loopbackHosts`   | `ReadonlyArray<string>`                                                                                  | The addresses that need no opt-in.                                                                                              |
| `refusal`         | `(bind: Bind) => string \| undefined`                                                                    | Why a bind is refused: a non-loopback host needs `listen` and credentials.                                                      |
| `url`             | `(bind: Bind) => string`                                                                                 | The URL the app connects to.                                                                                                    |
| `banner`          | `(bind: Bind, directory: string) => string`                                                              | The line printed once the server listens.                                                                                       |
| `databasePath`    | `(directory: string) => string`                                                                          | `<directory>/.smithers/opencode.sqlite`.                                                                                        |
| `BindRefused`     | `TaggedError`                                                                                            | A bind the rule refuses; `layer` and `host` fail with it.                                                                       |
| `shutdownTimeout` | `Duration.Input`                                                                                         | Two seconds: how long the socket waits on shutdown after the event streams were ended.                                          |
| `Options`         | `{ directory, bind, version, seat, agent?, heartbeat?, evaluator?, environment?, maxFrames?, pricing? }` | How the server is assembled; the evaluator defaults to `Health.evaluatorLayer` over `environment`.                              |
| `app`             | `(options) => Layer<never, never, HttpRouter \| Driver \| Store>`                                        | The routes behind CORS and auth over the hub and the turns, for an in-process handler.                                          |
| `layer`           | `(options) => Layer<HttpServer, ServeError \| BindRefused, Driver \| Store>`                             | The application on a Node socket over the host's driver and store. Ends the event streams, then closes the socket, on shutdown. |
| `host`            | `(options) => Effect<never, ServeError \| BindRefused, Driver \| Store>`                                 | Hosts the server until interrupted.                                                                                             |

## `Ownership`

One directory is one server's. A second server over a directory a live server
already holds refuses to start, because the two share the directory's store and
do not share their events. The record names the owner the way the engine names
the owner of a run, and liveness is the engine's own probe: a process on this
host is alive unless the operating system reports no such process, and a record
written on another host names a process this host cannot ask about.

| Export         | Signature                                                  | Meaning                                                                                          |
| -------------- | ---------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| `Claim`        | `{ owner: OwnerId, url }`                                  | The server that holds a directory, and the address it serves.                                    |
| `ClaimRefused` | `TaggedError`                                              | A directory another live server holds; the message is the refusal.                               |
| `claimPath`    | `(directory: string) => string`                            | `<directory>/.smithers/opencode.server.json`.                                                    |
| `read`         | `(directory: string) => Claim \| undefined`                | The record on disk, and nothing when there is none or what is there is not one.                  |
| `refusal`      | `(held: Claim, directory: string) => string`               | The sentence a second server refuses with: which server holds it, and the three ways out.        |
| `live`         | `(held: Claim, claimant: OwnerId) => Effect<boolean>`      | Whether the server the record names is still there.                                              |
| `Options`      | `{ directory, url, owner? }`                               | How the claim identifies this server; a fresh nonce on this host and process by default.         |
| `claim`        | `(options: Options) => Effect<Claim, ClaimRefused, Scope>` | Takes the directory, and gives it back when the scope closes. Replaces a killed server's record. |

## `Routes`

| Export        | Signature                                                                  | Meaning                                                                                         |
| ------------- | -------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| `Options`     | `{ directory, version, seat, agent }`                                      | How the routes describe the server.                                                             |
| `layer`       | `(options) => Layer<never, never, HttpRouter \| Store \| Events \| Turns>` | Mounts every route.                                                                             |
| `projectID`   | `(directory: string) => string`                                            | A stable hash of the absolute path.                                                             |
| `modelOf`     | `(seat: string) => ModelRef`                                               | `provider:model` split into `{ providerID, modelID }`.                                          |
| `slugOf`      | `(id: string) => string`                                                   | Two words from a session id.                                                                    |
| `provider`    | `(seat: string) => Record<string, unknown>`                                | The one provider entry with every field the app reads.                                          |
| `agent`       | `(name: string, seat: string) => Record<string, unknown>`                  | The one agent entry.                                                                            |
| `gitBranch`   | `(directory: string) => string \| undefined`                               | The branch from `.git/HEAD`.                                                                    |
| `listFiles`   | `(directory: string, path: string) => Array<FileNode>`                     | Entries of a directory under the served one.                                                    |
| `fileContent` | `(directory: string, path: string) => FileContent \| undefined`            | A regular file under `directory` as `text`, or `binary` with no content; `undefined` otherwise. |

Routes served: `/global/health`, `/api/health`, `/global/config`, `/config`,
`/path`, `/project`, `/project/current`, `PATCH /project/:id`, `/provider`,
`/agent`, `/command`, `/lsp`, `/skill`, `/formatter`, `/provider/auth`,
`/mcp`, `/experimental/resource`, `/question`,
`/permission`, `/vcs`, `/vcs/diff`, `/vcs/status`, `/find/file`, `/file`,
`/file/content`, `/api/reference`, `/session/status`, `/api/session`,
`GET|POST /session`, `GET|PATCH|DELETE /session/:id`, `/session/:id/message`,
`/session/:id/message/:messageID`, `/session/:id/todo`,
`/session/:id/children`, `/session/:id/diff`, `POST /session/:id/prompt_async`,
`POST /session/:id/abort`, `POST /session/:id/permissions/:permissionID`,
`/global/event` and `/event`. Any other route answers the JSON 404
`Cors.routeNotFound`, with the allow headers when the origin is allowed.

`GET /session/:id/message` pages on `limit` and `before`. A page that leaves
older messages behind names the oldest id on it in `X-Next-Cursor` and a
relative `Link` with `rel="next"`, both exposed to the browser. A `before`
that is not a message id of that session is a 400, not an empty page, so a
broken cursor is reported rather than read as the end of the history.

## `Events`

| Export             | Signature                                                     | Meaning                                                                                                                                                                     |
| ------------------ | ------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Envelope`         | `{ directory?, project?, payload: { id, type, properties } }` | The wire envelope of one event.                                                                                                                                             |
| `Options`          | `{ directory, project, heartbeat?, replay? }`                 | How the hub is built.                                                                                                                                                       |
| `Service`          | `{ publish, replay, stream, close }`                          | Stamp and broadcast an event; read the replay buffer; one SSE body (`{ after?, bare? }`), its live queue bounded to `replay` events, sliding; end every stream on shutdown. |
| `Events`           | `Context.Service`                                             | The hub service.                                                                                                                                                            |
| `make`, `layer`    | `(options) => Effect<Service>`, `(options) => Layer<Events>`  | Constructors.                                                                                                                                                               |
| `frame`            | `(envelope: Envelope, bare?) => string`                       | One `data:` frame: the envelope, or with `bare` its payload alone (the `/event` shape).                                                                                     |
| `heartbeatComment` | `string`                                                      | `: heartbeat`.                                                                                                                                                              |
| `defaultHeartbeat` | `Duration.Input`                                              | Fifteen seconds.                                                                                                                                                            |
| `defaultReplay`    | `number`                                                      | 256 events.                                                                                                                                                                 |

## `Store`

| Export                    | Signature                                                      | Meaning                                                                   |
| ------------------------- | -------------------------------------------------------------- | ------------------------------------------------------------------------- |
| `StoreError`              | `TaggedError`                                                  | The database refused a statement.                                         |
| `Grant`                   | `{ sessionID, kind: always \| once \| reject, key }`           | A permission decision kept across restarts.                               |
| `Turn`                    | `{ sessionID, messageID, prompt, history?, agent?, model? }`   | A turn the engine driver has not seen settle.                             |
| `Service`                 | sessions, messages, parts, permissions, grants, turns, `apply` | What the store does; `apply` persists what an emitted event implies.      |
| `Store`                   | `Context.Service`                                              | The store service.                                                        |
| `putHealth`, `listHealth` | on `Service`                                                   | One `flows.opencode.health.v1` record per decision, per session.          |
| `listParts`               | on `Service`: `(messageID) => Effect<Array<Part>, StoreError>` | The parts of one message in id order.                                     |
| `listMessages`            | on `Service`: `(sessionID, { limit?, before? })`               | One page of messages oldest first, each with the parts of that page.      |
| `migrations`              | `Migrations.MigrationSet`                                      | The `opencode` namespace under the shared `flows_migrations` ledger.      |
| `make`                    | `Effect<Service, StoreError, SqlClient>`                       | Builds the store over an ambient `SqlClient`.                             |
| `layer`                   | `Layer<Store, StoreError, SqlClient>`                          | The store over an ambient client.                                         |
| `layerSqlite`             | `(filename: string) => Layer<Store, StoreError>`               | The store over its own file.                                              |
| `defaultMessageLimit`     | `number`                                                       | 20.                                                                       |
| `databasePath`            | `(directory) => string`                                        | `<directory>/.smithers/opencode.sqlite`, the one file every module means. |

## `Projection`

| Export                                                                                | Meaning                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Context`, `Opened`, `State`, `Step`, `Closing`                                       | What the fold needs, what opens a turn, its state, its answer, and how a turn ends without an event.                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `open`                                                                                | `(ctx, opened) => Step`: the user message with the app's own part id (`opened.userPartID`), the session title, the assistant header, the busy status; `createdAt` and `userCreatedAt` keep a re-opened or retried turn's times.                                                                                                                                                                                                                                                                                                                                 |
| `fold`                                                                                | `(ctx, state, event: AgentEvent) => Step`: total; an unknown event changes nothing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `budgetEnded`                                                                         | `(state) => boolean`: whether the frame budget, and not the run, ended a turn that resolved. A run that finished applies a `complete` transition and the controller closes the turn on its words; a run out of budget applies none and gets the budget notice instead. Read off the transition and never off the notice.                                                                                                                                                                                                                                        |
| `claimTitle`, `claimText`                                                             | `(complete, overclaims) => string`, `(completion \| undefined) => string`: the completion brake's card. The title carries the two probabilities Jev read, and the body carries what to do and the completion the brake handed back, word for word, so a correct answer it refused is in the transcript instead of only in `opencode.sqlite`.                                                                                                                                                                                                                    |
| `close`                                                                               | `(ctx, state, closing) => Step`: ends a turn the stream did not end; every open cell and call card settles as an error, and a refused key is a `ProviderAuthError`. A turn that ended without an answer is red and the reason names what ended it, off a typed code every time: the provider's normalized code for a usage limit, the harness's `HarnessError` code for a cap or a refusal it made, `frame_budget` when the loop's top-of-frame budget check ended it, and `unknown` for a body that failed with neither. Only the operator's own Stop is gray. |
| `toolName`, `toolInput`, `toolTitle`, `toolOutput`                                    | How a flow call becomes the card OpenCode renders.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `toolMetadata`, `permissionPatterns`                                                  | The structured metadata beside a card, and the patterns of a permission card.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `bashSubject`, `unnameableBash`                                                       | `(input) => { command, always }`, `string`: what a bash call asks for, from any input `@smthrs/agent/std/Bash` accepts. A command line names its program in its first word, and a container qualifies it. A script is program text an interpreter reads on standard input, so it has no first word: the subject names the interpreter and the program's first line, and the card offers no `always` at all.                                                                                                                                                     |
| `cardInput`                                                                           | `(description, rest?) => Record<string, unknown>`: the input a card this server invents carries. Neither client reads a generic card's `title`, so the one line a reader needs goes in `description`, ahead of the short scalars that qualify it, and anything long stays in the metadata.                                                                                                                                                                                                                                                                      |
| `prose`, `answerText`, `chunks`                                                       | The model's prose outside code fences, the final answer, and its streamed deltas.                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `slots`, `finalFrame`, `summarySlot`, `demandOrdinals`                                | The sort keys parts are derived from.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `isClassify`, `classifyEntries`, `classifyAnswers`, `classifyOutput`, `classifyTitle` | How a classify call becomes the `classify` card: one line per state, the full answers as metadata.                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `classifyDoor`                                                                        | `(flowName) => string`: the curated id (`triage/relevance`) or `ad hoc`, which leads the classify card's title.                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `classifyDescription`                                                                 | `(flowName, input, value?) => string`: the line a classify card leads with. The door, and the leading answer to the first question the classifier declares; a batch names how many states it judged and a refused state names its code.                                                                                                                                                                                                                                                                                                                         |
| `Step.health`                                                                         | The `Health.Facts` a folded event hands out when it triggers an evaluation: a cell settling for the first time, a park, an aborted close. A frame the journal replays after a park hands out nothing and counts nothing twice.                                                                                                                                                                                                                                                                                                                                  |
| `decided`, `health`                                                                   | Fold a decision in: the dot on the title and a `health` card on a color change, sorted under the frame whose settlement or park produced the facts; `health` adds the time and the spend when the gateway answered the call, keeps the answers for the turn's final decision, and puts a failure on the card; the call itself was counted where it was asked. A turn's last reading is exempt from the color change rule and always writes its card, so the reason names what ended the run rather than what it was waiting for.                                |
| `replied`                                                                             | `(state) => Step`: the park a person just answered, cleared, and the color asked again from the same frame. `parked` is a fact about right now and the rule reads it before any answer, so a dot left on the old fact says "waiting for approval" about a session waiting for nobody. A session that is not parked is unchanged and asks nothing.                                                                                                                                                                                                               |
| `adopt`                                                                               | `(state, stored: Session) => State`: the title and the archive stamp as the store holds them now, so a rename or an archive made mid-turn is what the turn's next `session.updated` carries; an archived session carries no dot.                                                                                                                                                                                                                                                                                                                                |
| `Pricing`, `costOf`, `sessionNow`                                                     | Dollars per million tokens, the cost of a token count, and the session with this turn's tokens and cost, each frame counted once across a replay. The assistant header carries the last model step's tokens, the way OpenCode reports them.                                                                                                                                                                                                                                                                                                                     |
| `Summary`, `summaryLine`                                                              | The run summary: frames, calls, classify calls, Jev calls, latency, and spend, each counted once across a replay. `jevCalls` is every Jev call the run made: the classify calls, the health evaluation each trigger asks for, and the completion brake's reading at each completion attempt. Each is counted where it is asked, so a health answer that arrives after the turn ended is still one of the calls the footer reports. `jevLatencyMs` and `jevCost` add only what the gateway answered.                                                             |
| `defaultMaxFrames`, `lastCallsKept`, `healthTextCap`                                  | What the health facts carry; the frame budget follows the engine's `discipline-armed` event once it arrives.                                                                                                                                                                                                                                                                                                                                                                                                                                                    |

## `Health`

| Export                                                                                 | Signature                                                                                                          | Meaning                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| -------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `State`, `Facts`, `toState`                                                            | schema, `State & { demandThisFrame, stoppedBy, endedBy }`, `(facts) => State`                                      | What Jev is sent (design section 3.1), and the three server-only facts the rule also reads.                                                                                                                                                                                                                                                                                                                                                                                       |
| `Limit`, `limitReached`, `limitReason`                                                 | `{ code, seat }`, `(ProviderFailure \| undefined) => Limit \| undefined`, `(limit) => string`                      | The usage limit that ended a run, read off the provider's normalized `quota_exceeded` or `rate_limited` code and never off its sentence, and the red reason it renders as.                                                                                                                                                                                                                                                                                                        |
| `Ended`, `frameBudget`, `endedReasons`, `endedReason`                                  | `{ code, maxFrames? }`, `"frame_budget"`, a record total over `HarnessError.HarnessErrorCode`, `(ended) => string` | What ended a run the harness ended rather than the run: a `HarnessError` code, `frameBudget` for a spent frame budget, or `unknown` for a body that failed with neither. Every reason starts `stopped:`, and the record is total, so a code the harness adds is a type error here and never a run that ends with no reason a person can read.                                                                                                                                     |
| `answeredReason`                                                                       | `"answered"`                                                                                                       | The green reason a turn that resolved with nothing to judge it renders: rule 6's second clause is a fact and needs no answers, so a turn that finishes in one frame ends with a color instead of none.                                                                                                                                                                                                                                                                            |
| `classifier`                                                                           | `Classifier<"harness/health", State, questions>`                                                                   | The curated health classifier: `progress` (score), `stuck`, `needsHuman` (boolean).                                                                                                                                                                                                                                                                                                                                                                                               |
| `Answers`, `Color`, `Decision`                                                         | types                                                                                                              | The typed answers, `green \| yellow \| red \| gray`, and a color with its reason.                                                                                                                                                                                                                                                                                                                                                                                                 |
| `decide`, `arrived`                                                                    | `(facts, answers \| undefined) => Decision`, `(answers) => boolean`                                                | The color rule, design section 3.3, first match wins in three blocks: the facts (parked, stopped by a limit, ended by the harness, a demand this frame), then whether there is a confident answer at all (gray when there is not), then the answers. Among the answers, needing a person beats everything, arrival beats repetition (`arrived`, a confident `done`, or a turn whose last transition was `complete`), then repetition, then four frames of exploring with no edit. |
| `dots`, `colorOf`, `strip`, `dotted`, `retitle`                                        | title helpers                                                                                                      | The dot per color, and how a title carries, loses, and keeps one across a rename; `strip` drops every leading dot with the U+FE0F the app writes after it, because a rename can arrive under two or three of them; `retitle` drops a dot the rename arrives with and keeps the session's own, and an empty rename keeps the title.                                                                                                                                                |
| `evaluate`                                                                             | `(facts, deadline?) => Effect<Evaluation, never, Evaluator>`                                                       | One evaluation within the deadline; never fails. A transport failure is a gray decision whose reason is `unavailable(message)`.                                                                                                                                                                                                                                                                                                                                                   |
| `unavailable`                                                                          | `(message: string) => string`                                                                                      | The gray reason a failed transport renders: `health unavailable: <message>`, so the card says why and the way out (`noGatewayKey` without a key).                                                                                                                                                                                                                                                                                                                                 |
| `Evaluation`, `Entry`, `recordType`                                                    | `{ decision, answers, latencyMs, usage, error, code, answered }`, the stored record                                | What an evaluation produced, and the `flows.opencode.health.v1` record the store keeps. `code` is the transport's own `EvaluatorErrorCode`, so a caller decides on the code and not on the sentence.                                                                                                                                                                                                                                                                              |
| `gatewayAnswered`                                                                      | `(EvaluatorError \| undefined) => boolean`                                                                         | Whether the gateway answered a call, which is what makes its milliseconds the run's. Only `unreachable` and `timeout` mean nothing came back; every other code is the gateway's own answer over HTTP.                                                                                                                                                                                                                                                                             |
| `evaluatorLayer`, `evaluatorConfigured`, `ambientEnvironment`, `noGatewayKey`          | `(environment, dispatcher?) => Layer<Evaluator>`, `(environment) => boolean`, `() => process.env`, `string`        | Jev through the Vercel gateway under `evaluatorRetry`, replacing the scoped connection pool before the next request after a transport failure, when `AI_GATEWAY_API_KEY` is set, else one that refuses with `noGatewayKey` and is not retried. `evaluatorConfigured` answers the same question before the layer is built, which is what the startup preflight asks.                                                                                                               |
| `RetryPolicy`, `evaluatorRetry`                                                        | `{ attempts, backoffMs, deadlineMs, budgetMs }`, `{ 3, 250, 2500, 8000 }`                                          | The retry every gateway evaluation on this host runs under: three requests, 250 ms apart, 2500 ms over each, 8000 ms over all of them. The gateway gets `deadlineMs` as its `timeoutMs`, in place of the library's 1500 ms.                                                                                                                                                                                                                                                       |
| `retryable`                                                                            | `(EvaluatorError) => boolean`                                                                                      | Whether a failure is worth asking again: `unreachable`, `timeout`, and `refused` carrying 429 or a 5xx. Never `invalid_question`, `invalid_answer`, `empty`, or `refused` carrying anything else, 401 and 403 included.                                                                                                                                                                                                                                                           |
| `retrying`, `retryingLayer`                                                            | `(Evaluator, policy?) => Evaluator`, `(Layer<Evaluator>, policy?) => Layer<Evaluator>`                             | The same evaluator, asked again when the failure was a blip, with the last failure the one that surfaces. The harness's completion brake has no fallback, so without this one 429 ends the turn as `completion_unjudged`.                                                                                                                                                                                                                                                         |
| `jevCost`, `jevInputPricePerMillion`, `renderAnswers`, `confidenceFloor`, `deadlineMs` | constants and conversions                                                                                          | Jev's price and the card's answer text.                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `deadlineMisses`, `missedDeadlines`                                                    | `3`, `(count) => string`                                                                                           | How many deadlines in a row Jev may miss before the dot goes gray, and the gray reason the third one writes. One missed deadline is a measurement that did not arrive, not health that is unavailable, so `Projection.health` keeps the color the run already had; three in a row is a transport that is not answering.                                                                                                                                                           |

## `Turns`

| Export                            | Signature                                                                                                                                                   | Meaning                                                                                                                                                    |
| --------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `PromptInput`                     | `{ sessionID, messageID?, agent?, model?, parts }`                                                                                                          | What the app sends on `prompt_async`.                                                                                                                      |
| `TurnsError`                      | `TaggedError` with `unknown_session`, `unknown_permission`, `empty_prompt`                                                                                  | The failures a turn request reports.                                                                                                                       |
| `Options`                         | `{ directory, agent, model, maxFrames?, pricing? }`                                                                                                         | Defaults a prompt inherits.                                                                                                                                |
| `Service`                         | `{ prompt, abort, permission, update, status }`                                                                                                             | The composition; `update` applies a rename or an archive in order with the turn's writes; `permission` answers a row whose turn is gone by taking it down. |
| `Turns`                           | `Context.Service`                                                                                                                                           | The service.                                                                                                                                               |
| `promptText`                      | `(parts) => string`                                                                                                                                         | The text of a prompt's parts.                                                                                                                              |
| `history`                         | `(messages, cap?) => string \| undefined`                                                                                                                   | The conversation tail a follow-up carries, with every turn the person stopped marked.                                                                      |
| `historyCap`                      | `number`                                                                                                                                                    | 4096 characters.                                                                                                                                           |
| `abortedError`, `stoppedTurn`     | `string`, `string`                                                                                                                                          | The error name a stopped turn's header carries, and what the tail says about it.                                                                           |
| `isLocked`                        | `(error: StoreError) => boolean`                                                                                                                            | Whether a store write found the database held by the engine.                                                                                               |
| `whileLocked`                     | `(effect: Effect<A, StoreError>) => Effect<A, StoreError>`                                                                                                  | Retries a store call while the database is held.                                                                                                           |
| `userPartID`                      | `(userMessageID, parts) => string`                                                                                                                          | The user text part's id: the app's own, else derived.                                                                                                      |
| `steerRetryDelay`                 | `Duration.Input`                                                                                                                                            | How a prompt the running turn could not take waits to open the next.                                                                                       |
| `storeRetryDelay`, `storeRetries` | `Duration.Input`, `number`                                                                                                                                  | How a locked write is retried.                                                                                                                             |
| `make`, `layer`                   | constructors over `Driver`, `Store`, `Events` and `Evaluator`; `make` re-opens the turns the driver finds at boot and forks a health evaluation per trigger |                                                                                                                                                            |

## `Driver`

| Export            | Signature                                                                           | Meaning                                                                                                                                                              |
| ----------------- | ----------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `StartInput`      | `{ sessionID, messageID, prompt, history?, agent?, model? }`                        | What starts a turn; `messageID` is the execution id, `history` the conversation tail.                                                                                |
| `Outcome`         | `completed \| interrupted \| suspended \| failed`                                   | How the body ended; `failed` carries a `provider` when the seat refused the call and a `harness` when the harness itself ended the run.                              |
| `ProviderFailure` | `{ seat, providerID, code, status?, message }`                                      | The seat, the normalized code, the HTTP status, and the provider's message verbatim.                                                                                 |
| `HarnessStop`     | `{ code: HarnessErrorCode }`                                                        | The harness's own code, carried beside `ProviderFailure` because the two are different things to fix. Only the code travels: the sentence is on `message`, verbatim. |
| `Sink`            | `{ event, closed }`                                                                 | Where a running turn reports.                                                                                                                                        |
| `PermissionInput` | `{ sessionID, permissionID, response }`                                             | A permission answer.                                                                                                                                                 |
| `DriverError`     | `TaggedError` with `busy`, `unknown_session`, `unknown_permission`, `engine_failed` | The failures a driver reports.                                                                                                                                       |
| `Service`         | `{ start, interrupt, permission, steer, resumeOnBoot }`                             | What a driver does.                                                                                                                                                  |
| `Driver`          | `Context.Service`                                                                   | The seam.                                                                                                                                                            |

## `EngineDriver`

| Export                                                             | Signature                                                                                                                | Meaning                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Host`                                                             | `{ platform, seats, registry }`                                                                                          | What the host equips a turn with: the guarded platform, the seat resolver, the registry.                                                                                                                                                                                                                                                                                                                                                                            |
| `FlowServices`                                                     | `{ filesystem, shell, engine, evaluator }`                                                                               | The service slices the standard flows are built from.                                                                                                                                                                                                                                                                                                                                                                                                               |
| `Options`                                                          | `{ directory, seat, host, maxFrames?, limits?, flows?, asks?, databaseFile?, attachTimeout?, evaluator?, environment? }` | How the driver is built; the evaluator defaults to `Health.evaluatorLayer` over `environment`.                                                                                                                                                                                                                                                                                                                                                                      |
| `layer`                                                            | `(options) => Layer<Driver \| Store>`                                                                                    | The driver and the store over one engine database.                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `readOnlyCap`                                                      | `6`                                                                                                                      | Consecutive read-only frames a turn may spend: the harness demands an action at six and ends the run at twelve (`read_only_cap`, a red `stopped` decision). Half the harness default; a turn in the app is a person waiting.                                                                                                                                                                                                                                        |
| `hostTeaching`                                                     | `string`                                                                                                                 | The host's system teaching every turn carries: call `ctx.done` in the cell that has the answer, call flows only when needed, never run a command the person did not ask for.                                                                                                                                                                                                                                                                                        |
| `deniedMessage`                                                    | `(flow) => string`                                                                                                       | The message a rejected call settles with; says the harness's generic `capability_refused` hint beside it is about the flow, not this call.                                                                                                                                                                                                                                                                                                                          |
| `repeatedDenialMessage`                                            | `(flow, subject) => string`                                                                                              | What a call settles with when this turn already had the same subject rejected, instead of asking the person a second time.                                                                                                                                                                                                                                                                                                                                          |
| `turnFlow`                                                         | `Flow`                                                                                                                   | The one durable flow every prompt executes, `opencode/turn`.                                                                                                                                                                                                                                                                                                                                                                                                        |
| `task`                                                             | `(input: StartInput) => string`                                                                                          | The task the model is given: the prompt, behind the conversation tail when there is one.                                                                                                                                                                                                                                                                                                                                                                            |
| `requestID`                                                        | `(executionId, identity) => string`                                                                                      | The permission id of a call, stable across replay.                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `standardFlows`                                                    | `(services) => ReadonlyArray<FlowBinding.Source>`                                                                        | Filesystem, shell, clock, and classify with the three curated classifiers.                                                                                                                                                                                                                                                                                                                                                                                          |
| `refusing`                                                         | `(source, refusal) => FlowBinding.Source`                                                                                | Settles a call the refusal function names as `capability_refused`, with the message it returns.                                                                                                                                                                                                                                                                                                                                                                     |
| `envelope`                                                         | `ReadonlyArray<string>`                                                                                                  | `fs:read:/**`, `fs:write:/**`, `proc:spawn:*`, `model:call:*`.                                                                                                                                                                                                                                                                                                                                                                                                      |
| `defaultLimits`                                                    | `Sandbox.Limits`                                                                                                         | The CLI's cell budget; the frame budget is `Projection.defaultMaxFrames`.                                                                                                                                                                                                                                                                                                                                                                                           |
| `alwaysKey`                                                        | `(directory, flow, input) => string \| undefined`                                                                        | What Allow always grants: the flow and the card's pattern, `bash echo *`. `undefined` when the card offered no pattern, and then the answer covers that one call and nothing else.                                                                                                                                                                                                                                                                                  |
| `cardSubject`                                                      | `(directory, flow, input) => string`                                                                                     | What a call's card shows as its subject. Two calls with the same subject ask the person the same question, which is what makes a second ask of a rejected one a nag.                                                                                                                                                                                                                                                                                                |
| `evaluatorRefusal`, `noEvaluator`                                  | `(options) => string \| undefined`, `string`                                                                             | The startup preflight: why this host cannot run a turn, or `undefined`. A host that injects its own evaluator passes; a host that leaves the evaluator to its environment is refused when that environment holds no key.                                                                                                                                                                                                                                            |
| `failedOutcome`, `seatFailureLine`                                 | `(seat, cause) => Outcome`, `(failure) => string`                                                                        | A refused seat as an outcome with the provider's words, and the server log line with the hint.                                                                                                                                                                                                                                                                                                                                                                      |
| `judgeRefusal`, `judgeSeat`, `judgeProviderID`, `judgeFailureLine` | `(cause) => ProviderFailure \| undefined`, `string`, `string`, `(failure) => string`                                     | A turn the gateway refused to judge, typed. `completion_unjudged` reached the app as a bare `UnknownError`; now it carries the judge's own seat, the normalized code off the transport's (401 and 403 are `authentication`, 429 is `rate_limited`), the status, and the words verbatim, which the projection renders as `ProviderAuthError`. A gateway that answered nothing names no provider. The log line points at `AI_GATEWAY_API_KEY`, never at another seat. |
| `harnessStop`                                                      | `(error) => HarnessStop \| undefined`                                                                                    | The `HarnessError` code inside a failure, live or as the JSON projection a replayed failure arrives as, and `undefined` for anything outside `HarnessErrorCode`. `failedOutcome` kept only the `ModelError` it found and dropped this, so every cap and every refusal the harness makes reached the projection as an untyped sentence and the dot went gray reading `failed`.                                                                                       |
| `databasePath`                                                     | `(directory) => string`                                                                                                  | `<directory>/.smithers/opencode.sqlite`.                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `inertJj`                                                          | `{ snapshot, restore, diff }`                                                                                            | The version control the engine is built over; a turn never calls it.                                                                                                                                                                                                                                                                                                                                                                                                |

## `ScriptedDriver` and `DemoScript`

| Export                         | Meaning                                                                                           |
| ------------------------------ | ------------------------------------------------------------------------------------------------- |
| `ScriptedDriver.Segment`       | A run of events, or a permission park with its `allowed` and `rejected` continuations.            |
| `ScriptedDriver.Script`        | `{ segments }`.                                                                                   |
| `ScriptedDriver.Options`       | `{ script, delay? }`: the turn to replay and the pause between events.                            |
| `ScriptedDriver.make`, `layer` | Constructors.                                                                                     |
| `DemoScript.script`            | `(input: StartInput) => Script`: the recorded turn keyed by the session id, with a classify call. |

## `Ids`

| Export     | Signature                                                                          | Meaning                                                                        |
| ---------- | ---------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| `prefixes` | `{ session: "ses", message: "msg", part: "prt", permission: "per", event: "evt" }` | OpenCode's prefixes.                                                           |
| `make`     | `(kind, timestamp?) => string`                                                     | A fresh id; sessions sort newest first, the rest in creation order.            |
| `part`     | `(messageID, { frame, slot, ordinal }) => string`                                  | A part id derived from its message and a sort key.                             |
| `reply`    | `(userMessageID) => string`                                                        | The answer's id, derived from the prompt's so a retry runs the same execution. |
| `head`     | `(timestamp, descending, sequence) => string`                                      | The twelve-character time head.                                                |
| `isKind`   | `(kind, id) => boolean`                                                            | Whether an id carries a kind's prefix.                                         |

## `Pricing`

| Export      | Signature                                   | Meaning                                                                 |
| ----------- | ------------------------------------------- | ----------------------------------------------------------------------- |
| `pricingOf` | `(seat) => Projection.Pricing \| undefined` | The published list price of a starter seat; an unknown seat costs zero. |

## `Cors` and `Auth`

| Export                 | Meaning                                                                                            |
| ---------------------- | -------------------------------------------------------------------------------------------------- |
| `Cors.defaultOrigins`  | `https://*.opencode.ai`, `http://localhost:*`, `http://127.0.0.1:*`.                               |
| `Cors.allows`          | `(origin, extras?) => boolean`.                                                                    |
| `Cors.layer`           | `(extras?) => Layer`: answers preflights and stamps the allow headers, on a 404 too.               |
| `Cors.preflightVary`   | `Origin, Access-Control-Request-Headers`: what a preflight answer varies on, since it echoes both. |
| `Cors.routeNotFound`   | The JSON 404 body an unmounted route answers: `{ name: "NotFoundError", data: { message } }`.      |
| `Auth.Credentials`     | `{ username, password }`.                                                                          |
| `Auth.fromEnvironment` | Reads `OPENCODE_SERVER_PASSWORD` and `OPENCODE_SERVER_USERNAME`.                                   |
| `Auth.authorizes`      | `(header, credentials) => boolean`.                                                                |
| `Auth.layer`           | `(credentials \| undefined) => Layer`: 401 on everything but the health probes and preflights.     |
