// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: canary_results.sql

package clusterdb

import (
	"context"
	"time"
)

const listCanaryResults = `-- name: ListCanaryResults :many

SELECT id, suite, test_name, status, duration_seconds, error_message, run_id, reported_at, created_at, updated_at
FROM canary_results
ORDER BY suite ASC, test_name ASC
`

// Private cluster queries kept separate from the product graph.
func (q *Queries) ListCanaryResults(ctx context.Context) ([]CanaryResult, error) {
	rows, err := q.db.Query(ctx, listCanaryResults)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []CanaryResult{}
	for rows.Next() {
		var i CanaryResult
		if err := rows.Scan(
			&i.ID,
			&i.Suite,
			&i.TestName,
			&i.Status,
			&i.DurationSeconds,
			&i.ErrorMessage,
			&i.RunID,
			&i.ReportedAt,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const upsertCanaryResult = `-- name: UpsertCanaryResult :one
INSERT INTO canary_results (
    suite,
    test_name,
    status,
    duration_seconds,
    error_message,
    run_id,
    reported_at
)
VALUES ($1, $2, $3, $4, $5, $6, $7)
ON CONFLICT (suite, test_name)
DO UPDATE SET
    status = EXCLUDED.status,
    duration_seconds = EXCLUDED.duration_seconds,
    error_message = EXCLUDED.error_message,
    run_id = EXCLUDED.run_id,
    reported_at = EXCLUDED.reported_at,
    updated_at = NOW()
RETURNING id, suite, test_name, status, duration_seconds, error_message, run_id, reported_at, created_at, updated_at
`

type UpsertCanaryResultParams struct {
	Suite           string    `json:"suite"`
	TestName        string    `json:"test_name"`
	Status          string    `json:"status"`
	DurationSeconds float64   `json:"duration_seconds"`
	ErrorMessage    string    `json:"error_message"`
	RunID           string    `json:"run_id"`
	ReportedAt      time.Time `json:"reported_at"`
}

func (q *Queries) UpsertCanaryResult(ctx context.Context, arg UpsertCanaryResultParams) (CanaryResult, error) {
	row := q.db.QueryRow(ctx, upsertCanaryResult,
		arg.Suite,
		arg.TestName,
		arg.Status,
		arg.DurationSeconds,
		arg.ErrorMessage,
		arg.RunID,
		arg.ReportedAt,
	)
	var i CanaryResult
	err := row.Scan(
		&i.ID,
		&i.Suite,
		&i.TestName,
		&i.Status,
		&i.DurationSeconds,
		&i.ErrorMessage,
		&i.RunID,
		&i.ReportedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
