name: toon-retry

input:
  name: string

steps:
  - id: flaky
    run: |
      if (attempt < 3) {
        throw new Error("flaky");
      }
      return { ok: true };
    output:
      ok: boolean
    retry:
      maxAttempts: 3
      backoff: fixed
      initialDelay: 30ms
