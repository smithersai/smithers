export default function Prompt(props) {
  const { children, components, ...data } = props ?? {};
  return (
    <>
      {"Example prompt: failing test author author"}
      {"\n\n"}
      {JSON.stringify(data, null, 2)}
    </>
  );
}
