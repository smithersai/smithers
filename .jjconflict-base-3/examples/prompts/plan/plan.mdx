Analyze the codebase at "{props.directory}" and create an implementation plan for:

Goal: {props.goal}

Requirements:
{props.requirements ?? "See codebase for context"}

Constraints:
{props.constraints ?? "None specified"}

Break this into small, parallelizable tasks. Identify dependencies and critical path.
