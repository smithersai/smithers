# Cargo workspace for the Rust side of flows.
#
# The pinned jj fork is a git dependency of `crates/flows-jj`, so cargo fetches
# and builds it out of its own checkout; nothing about it lives in this tree.

[workspace]
resolver = "3"
members = ["crates/flows-jj", "crates/smithers-ffi"]

[workspace.dependencies]
# Both browser flows and the repository service use the same jj store format.
jj-lib = { git = "https://github.com/smithersai/jj.git", rev = "47589ada70c12b3e829b5c98ab32503abad49eac", default-features = false }

# No debuginfo in dev builds: jj-lib's debuginfo multiplies the target dir
# several times over, and this workspace is exercised through tests, not
# debuggers.
[profile.dev]
debug = false

# The release profile tunes for the wasm32-wasip1 artifact that ships to the
# browser: small and predictable beats fast-to-compile.
[profile.release]
codegen-units = 1
lto = "thin"
opt-level = "s"
strip = "debuginfo"
