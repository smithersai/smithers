You are player {props.currentPlayer}. Here is the current board:

{props.board}

Available positions (row, col): {props.availablePositions}

Pick an open position. Return ONLY JSON: {`{"row": <0-2>, "col": <0-2>}`}
