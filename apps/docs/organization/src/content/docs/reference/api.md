---
title: "API reference"
description: "Every public export of @smthrs/organization: profile schemas, grant checks, roster loading and validation, prompt composition, skill packs, hiring, meeting planning, configuration, gates, role-task authority, budgets, role hosts, microVM workspaces, and durable actions."
editUrl: "https://github.com/smithersai/smithers/edit/main/packages/smithers/agent/organization/docs/api.md"
---

Every module is importable from the root as a namespace
(`import { Roster } from "@smthrs/organization"`) and from its own subpath
(`@smthrs/organization/Roster`). Functions that can refuse return a `Result`
or an `Effect` failing with the module's tagged error; each error carries a
stable `code` or `reason` and a message that never repeats a field's value.
Loaders need `FileSystem` and `Path` from `effect`; the workspace also needs a
`ChildProcessSpawner` for host `git`.

## `@smthrs/organization/Profile`

| Export                                                                                                                                            | Behavior                                                                                                                                          |
| ------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Profile`                                                                                                                                         | The complete role profile schema: id, name, kind, status, version, `reportsTo`, seat, charter, grants, budget, memory, skills, cases, identities. |
| `Grants`, `ConnectionGrant`, `HiringLimits`                                                                                                       | Every authority a principal holds; anything not listed is not granted.                                                                            |
| `RetrievalScope`, `Domain`                                                                                                                        | `grants.retrieval`: optional `allow` and `deny` domain lists, each entry covering its subdomains.                                                 |
| `Charter`, `OutputField`                                                                                                                          | The charter sections and the declared output fields.                                                                                              |
| `BudgetPolicy`                                                                                                                                    | `tokensPerTask`, `tasksPerDay`, `concurrency`, optional `usdPerMonth`.                                                                            |
| `TaskContract`, `RoleResult`, `Evidence`                                                                                                          | What a principal is asked to do, and the one result schema every role task returns.                                                               |
| `PrincipalId`, `Superior`, `Kind`, `Status`, `Contact`, `Tool`, `Access`                                                                          | Identifier and enumeration schemas. `owner` is reserved.                                                                                          |
| `ReferenceName`, `Container`, `KnowledgeGrant`, `SkillName`, `BankName`, `Instant`, `Version`, `Seat`, `TaskId`, `Line`, `Paragraph`, `FieldName` | Field schemas. A reference shaped like a provider token is refused; a knowledge grant is a relative file or a `/`-terminated subtree.             |
| `decodeProfile`, `decodeGrants`, `decodeTaskContract`, `decodeRoleResult`                                                                         | Decoders that refuse unknown keys.                                                                                                                |
| `defaultMemoryNamespace(id)`                                                                                                                      | `agent-<id>`.                                                                                                                                     |
| `maxPrincipalIdLength`                                                                                                                            | 120.                                                                                                                                              |

## `@smthrs/organization/Grants`

| Export                                                                        | Behavior                                                                                                                                                                                                                |
| ----------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `widenings(child, parent, options?)`                                          | Every way `child` reaches beyond `parent`. With `hired` (the default) a child never holds personal accounts or owner contact. A child's retrieval `allow` stays inside the parent's and its `deny` covers the parent's. |
| `withinDomain(domain, scope)`                                                 | `domain` is `scope` or a subdomain of it.                                                                                                                                                                               |
| `subsetOf(child, parent, options?)`                                           | `widenings(...).length === 0`.                                                                                                                                                                                          |
| `isAssistant(profile)`                                                        | The active, unhired core principal with personal accounts and `owner-direct` contact.                                                                                                                                   |
| `canRead(principal, record, connections)`                                     | Refuses tombstones, personal records for anyone but the assistant, and ungranted containers.                                                                                                                            |
| `canWrite(principal, connection, container, connections)`                     | Requires write access on the container.                                                                                                                                                                                 |
| `canResolveCredential(principal, connection, connections)`                    | The connection is configured and granted; personal connections resolve only for the assistant.                                                                                                                          |
| `canReadKnowledge(principal, path)`                                           | A relative file path covered by a knowledge grant.                                                                                                                                                                      |
| `makeReceiptIssuer(issuedBy)`, `canContactOwner(principal, receipt, context)` | Owner contact: the assistant always; others only with an in-process receipt for that principal, destination, and window.                                                                                                |
| `Denied`, `DeniedReason`, `Widening`, `maxReceiptWindowMs`                    | The refusal, its reasons, one widening, and the longest receipt window (one day).                                                                                                                                       |

## `@smthrs/organization/Roster`

| Export                                                                                                          | Behavior                                                                                                                          |
| --------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| `parseProfile(path, text)`                                                                                      | Parses one profile file. Fails with `RosterError` codes `frontmatter`, `schema`, or `body`.                                       |
| `renderProfile(profile)`                                                                                        | The inverse of `parseProfile`: canonical key order and fixed section order.                                                       |
| `load(dir)`                                                                                                     | Loads `Roles/*.md` and `Specialists/*.md`, confined to the real path of `dir`, at most `maxProfileBytes` each. Does not validate. |
| `make(profiles, sources)`, `revisionOf(...)`                                                                    | Builds a roster; the revision is the SHA-256 of the canonical profiles and source digests, independent of input order.            |
| `validate(profiles, policy)`                                                                                    | Every broken invariant as a `Violation` with a `ViolationCode`. Empty means valid.                                                |
| `validateResult(profile, result)`                                                                               | Undeclared fields, and for `done`: missing declared fields and missing evidence.                                                  |
| `resolveActive(roster, id)`                                                                                     | The profile when it and every principal up its hiring chain are active; otherwise `Grants.Denied`.                                |
| `childrenOf`, `hireChain`, `isHired`, `dailyTokens`                                                             | Roster queries used by validation and hiring.                                                                                     |
| `RosterError`, `RosterErrorCode`, `Violation`, `ViolationCode`, `Policy`, `Roster`, `Source`, `maxProfileBytes` | Errors, codes, and models.                                                                                                        |

## `@smthrs/organization/Prompt`

| Export                                                                                                             | Behavior                                                                                                                                                                                                           |
| ------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `compose(input)`                                                                                                   | Composes `{ system, prompt, digest, parts }`, or fails with `PromptError` codes `skill-not-granted`, `skill-missing`, `duplicate-skill`, `invalid-context`, `part-too-large`, `total-too-large`, `invalid-limits`. |
| `renderCharter(profile)`, `renderTask(task)`                                                                       | The charter and task texts `compose` uses.                                                                                                                                                                         |
| `renderContext(entry)`, `dataNotice`                                                                               | Fences one context entry as escaped data.                                                                                                                                                                          |
| `defaultLimits`                                                                                                    | Common 8 KiB, charter 12 KiB, each skill 16 KiB, task 16 KiB, all context 48 KiB, total 192 KiB.                                                                                                                   |
| `Common`, `SkillText`, `ContextEntry`, `Part`, `PartKind`, `Limits`, `ComposeInput`, `Composed`, `PromptErrorCode` | Inputs and outputs.                                                                                                                                                                                                |

## `@smthrs/organization/Skills`

| Export                                                             | Behavior                                                                                                                                  |
| ------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------- |
| `loadPack(dir)`                                                    | Loads every `<name>/SKILL.md`, ignoring hidden entries and loose files, confined to the real path of `dir`, at most `maxSkillBytes` each. |
| `parseSkill(directory, text)`                                      | Checks keys, name, description, license, compatibility, `allowed-tools`, metadata, and adapted provenance.                                |
| `select(pack, names)`                                              | The `Prompt.SkillText` for each name, in order, pinned at its revision; `missing` when a name is not in the pack.                         |
| `revisionOf(skills)`                                               | The pack revision over every name and file digest.                                                                                        |
| `allowedLicenses`                                                  | `Apache-2.0`, `MIT`, `CC-BY-4.0`; an organization's own skills may carry `LicenseRef-<name>` instead.                                     |
| `SkillsError`, `SkillsErrorCode`, `Skill`, `Pack`, `maxSkillBytes` | Errors and models.                                                                                                                        |

## `@smthrs/organization/Hiring`

| Export                                                                                                            | Behavior                                                                                                                                                                                                                                                                                                                                                                                                              |
| ----------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `propose(request, roster, policy)`                                                                                | The `proposed` hire, or every violation it introduces (`invalid-profile`, `parent-inactive`, `grants-widen`, limits, `duplicate-id`...).                                                                                                                                                                                                                                                                              |
| `specialistId(parent, slug)`                                                                                      | `<parent>.<slug>`, refused past the id grammar.                                                                                                                                                                                                                                                                                                                                                                       |
| `transition(profile, action, at)`                                                                                 | `activate`, `pause`, `resume`, `retire`; retirement revokes grants and records `retiredAt`. Fails with `TransitionRefused`.                                                                                                                                                                                                                                                                                           |
| `retireWithHires(roster, id, at)`                                                                                 | Retires a principal and every unretired hire below it, root first.                                                                                                                                                                                                                                                                                                                                                    |
| `settleTask(roster, taskId, at)`                                                                                  | Retires the helpers scoped to a task and their hires.                                                                                                                                                                                                                                                                                                                                                                 |
| `RosterStore`, `layerFileSystem({ dir })`                                                                         | Stores hired profiles in `Specialists/<id>.md` atomically. `write(profile, expectedDigest?)` creates, or compare-and-sets on the digest.                                                                                                                                                                                                                                                                              |
| `fromSpec(parent, spec, at, taskScope?)`                                                                          | The `HireRequest` a role's structured `hire` field (`HireSpec`) asks for, with narrow defaults (no grants, `via-parent`, a budget of at most `defaultHireBudget` and never above the parent's; with the `retrieval` tool, the parent's web `allow` when none is named and the parent's `deny` always kept), or `invalid-profile` when the field does not decode. `propose` then refuses what the parent may not give. |
| `HireSpec`, `defaultHireBudget`                                                                                   | The `hire` field's schema (only `slug`, `name`, `objective`, `responsibilities` required) and the default hire budget.                                                                                                                                                                                                                                                                                                |
| `HireRequest`, `Slug`, `Action`, `revokedGrants`, `Stored`, `Service`, `RosterStoreError`, `RosterStoreErrorCode` | Inputs, errors, and models.                                                                                                                                                                                                                                                                                                                                                                                           |

## `@smthrs/organization/Meetings`

| Export                                                                                                                                                                               | Behavior                                                                                                                                           |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| `planWeekly(request)`                                                                                                                                                                | A contiguous weekly block; refuses a wrong weekday, repeated principals, a block past midnight, and any boundary in a gap within `gapHorizonDays`. |
| `occurrences(plan, fromMs, toMs)`                                                                                                                                                    | Every meeting starting in `[fromMs, toMs)`, ordered by start, at most `maxExpansionWeeks` weeks.                                                   |
| `occurrenceKey(eventKey, localDate)`                                                                                                                                                 | `<eventKey>@<local date>`, stable across recomputation.                                                                                            |
| `conflicts(intervals, busy)`                                                                                                                                                         | Overlapping pairs; touching intervals do not overlap.                                                                                              |
| `findSlot(request)`                                                                                                                                                                  | The first free grid-aligned interval in a weekly window within `maxHorizonDays`, skipping gap times.                                               |
| `WeeklyRequest`, `Weekday`, `LocalTime`, `LocalDate`, `SeriesId`, `Slot`, `WeeklyPlan`, `Occurrence`, `Interval`, `FindSlotRequest`, `FoundSlot`, `MeetingError`, `MeetingErrorCode` | Inputs, outputs, and errors.                                                                                                                       |

## `@smthrs/organization/Config`

| Export                                                                                 | Behavior                                                                                                                                                                                                                                                                 |
| -------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `load(root, file?)`                                                                    | Reads `Org/Organization.md` (or `file`) and the policy, connections, and meetings pages it names, confined to the real path of `root`.                                                                                                                                   |
| `parseOrganization`, `parseGatePolicy`, `parseConnections`, `parseMeetings`            | Parse one page's frontmatter; unknown keys are refused and values are never repeated in errors. Unsupported gate kinds are refused at policy load.                                                                                                                       |
| `weeklyRequest(meetings)`                                                              | The `Meetings.WeeklyRequest` a meetings page describes, or `undefined` while its inputs are unset.                                                                                                                                                                       |
| `environmentOf(entry)`                                                                 | The `Workspace.Environment` a `repositories.<name>` entry of the organization page declares; each check `run` becomes `sh -c <run>`.                                                                                                                                     |
| `RepositoryEnvironment`, `RepositoryCheck`, `RepositoryName`                           | The organization page's `repositories` map: per repository `prepare { run, key, network, timeoutMs? }`, `network` (`none`, `all`, or a domain list; default `none`), and `checks [{ name, run, timeoutMs? }]`. `vm.diskMib` sets the root disk of image-booted machines. |
| `Organization`, `Connection`, `Connections`, `Meetings`, `Seats`, `WikiPath`, `Loaded` | Page schemas and the loaded result.                                                                                                                                                                                                                                      |
| `ConfigError`, `ConfigErrorCode`, `maxPageBytes`, `defaultOrganizationFile`            | Errors and constants.                                                                                                                                                                                                                                                    |

## `@smthrs/organization/Gates`

| Export                                                                                                | Behavior                                                                                                                                                  |
| ----------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `before(policy, at, subject, next, options?)`                                                         | `next` unchanged when no gate matches `at`; otherwise the matching Approval and Review gates in policy order, then `next`. Unsupported kinds are refused. |
| `select(policy, at)`, `empty(revision)`                                                               | The gates attached at a boundary and target (`*` matches any target); an empty policy.                                                                    |
| `answer({ token, subjectDigest, approved, reason? })`, `pending(request)`                             | Record an authenticated person's approval answer; read a waiting row as an approval question.                                                             |
| `ReviewTask`, `reviewTask`, `Reviewer`, `ReviewRequest`, `ReviewVerdict`, `ReviewFailed`              | The Review gate's declared step, its default reviewer node, and its models. `GatesLive.layer({ review })` implements it.                                  |
| `GatePolicy`, `GateSpec`, `Approval`, `Review`, `Budget`, `Concurrency`, `Window`, `Condition`, `At`  | Policy schemas. Only Approval and Review run (`supportedKinds`, `unsupported`).                                                                           |
| `GateRecord`, `GateRefused`, `GateUnsupported`, `Outcome`, `Subject`, `Decide`, `Binding`, `Response` | The sealed decision record, the refusals, and the declared steps a gate is made of.                                                                       |

## `@smthrs/organization/GatesLive`

| Export                                 | Behavior                                                                                                               |
| -------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| `layer({ review? })`                   | Implements the gate steps and `HumanTask`. Without `review`, every Review gate is denied. See `Actions.reviewHandler`. |
| `bind(gate, subject)`, `subjectDigest` | The question name, prompt, and digest a gate binds its subject to.                                                     |
| `decide(gate, digest, response, at)`   | The pure decision: passed, denied, or expired, and who decided.                                                        |
| `ReviewHandler`, `maxExcerptLength`    | The reviewer a host supplies; the subject excerpt bound in a prompt.                                                   |

## `@smthrs/organization/Authority`

| Export                                                                                                                      | Behavior                                                                                                                                                                                                                                                                                         |
| --------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `loadSnapshot(root, loaded)`                                                                                                | Loads the roster (`rosterDir`), the skill pack (`skillsDir`, empty when unset), and the common instructions (`commonFile`, empty when unset) from a wiki root, confined by real path, and validates the roster against the pack.                                                                 |
| `makeSnapshot({ roster, common, skills, weeklyMeeting?, hireSeats? })`                                                      | Validates and pins; the revision covers the roster revision, the common text, and the pack revision. Fails with `AuthorityError` `invalid-roster` listing every violation.                                                                                                                       |
| `makeRegistry(initial)`, `layerRegistry(initial)`, `RosterRegistry`                                                         | The trusted registry: `pin(snapshot)` (keeps every pinned revision, makes it current), `current`, `get(revision)`, `resolve(revision, id)`. Only host code fills it.                                                                                                                             |
| `resolvePrincipal(pinned, current, id)`                                                                                     | The pinned profile when it and every hirer up its chain are active in both rosters and its pinned grants are still inside its current grants.                                                                                                                                                    |
| `RoleTaskPayload`, `TaskWorkspace`, `promptOf(payload)`                                                                     | A role task names `revision`, `principal`, `seat`, `task`, `context`, `digest`, and optionally a `workspace { key, repository }`. It never carries a profile, grants, or a system prompt.                                                                                                        |
| `compose(snapshot, profile, task, context)`                                                                                 | The principal's composition from the snapshot: common instructions, charter, and the profile's skills from the pack, then the task and context.                                                                                                                                                  |
| `authorize(payload, executionId)`                                                                                           | Every dispatch check. Refuses with `DispatchRefused` reasons `malformed-payload`, `unknown-revision`, `unknown-principal`, `inactive`, `grants-narrowed`, `seat-mismatch`, `composition-failed`, `composition-mismatch`, `workspace-not-granted`, `repository-not-granted`, `workspace-foreign`. |
| `layer(actions)`                                                                                                            | Wraps `FlowRuntime.register` and `Action.Implementations.add` for `organization/role-task` so every execution is authorized and runs under its `RoleHost` and `CapabilitySet.attenuate` of that host's envelope. A refusal is an `assembly_failed` `HarnessError` before any model call.         |
| `roleTaskTag`, `Snapshot`, `Registry`, `Authorized`, `AuthorityError`, `DispatchRefused`, `RefusalReason`, `maxCommonBytes` | Constants, models, and errors.                                                                                                                                                                                                                                                                   |

A role task is resolved again whenever its step executes, including on resume.
A host restarted with a different roster no longer holds the old revision, so a
run pinned to it is refused with `unknown-revision` rather than run under a
roster it was not composed against.

## `@smthrs/organization/Budgets`

| Export                                                                                  | Behavior                                                                                                                                                                                                                                                                                                                                 |
| --------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `layer(actions, { maxConcurrentTasks })`                                                | Wraps every `organization/role-task` registration (install it inside `Authority.layer`): charges the task to the principal's and every hirer's daily ledger, waits for a host permit and one of the principal's `budget.concurrency`, and runs it under `taskBudget`. A limit answers a `blocked` result naming it, never a silent stop. |
| `taskBudget(shared, principal, tokensPerTask)`                                          | A `Budget` service refusing calls once the task has spent, or would pass, its ceiling (`BudgetExceeded`, `fail`), delegating everything else to the run's budget (standing alone without one). Step keys count once.                                                                                                                     |
| `exhaustion(error)`, `blocked(profile, reason)`                                         | Whether a failure is a spent budget, and the `blocked` result a limit answers with (escalating to the assistant, or to the hire's parent).                                                                                                                                                                                               |
| `Ledger`, `makeLedgerMemory`, `layerLedgerMemory`, `layerLedgerFile({ file })`          | The daily task ledger: `charge` is all or nothing across the principals, once per task; the file ledger keeps `retainedDays` days and is written by rename.                                                                                                                                                                              |
| `Charge`, `Charged`, `LedgerService`, `LedgerError`, `Options`, `dayOf`, `retainedDays` | Models, errors, and the UTC day of an instant.                                                                                                                                                                                                                                                                                           |

## `@smthrs/organization/RoleHost`

| Export                                                                                                                                 | Behavior                                                                                                                                                                                                                                                                                                                                                                                                  |
| -------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `make(options)`                                                                                                                        | Builds one role task's `AgentAction.Host`: registry limited to the profile's skills; flows only for granted families (`memory`: scoped `StandardFlows.memory`; `wiki-read`: `wikiRead`; `retrieval`: `webFetch` and `serverTools`; `workspace`: standard file and shell flows over the workspace session); envelope = those flows' capabilities. A granted family without its resource fails composition. |
| `Resources`, `layerResources(resources)`                                                                                               | What the host lends role tasks: `memory` (store and recall), `wiki { root, services, maxBytes? }`, `skills` (a registry), `retrieval { network?, limits?, now?, logDir?, providerSearch? }`, `claimCap` (0 when the judge is `none`).                                                                                                                                                                     |
| `skillsRegistry(base, names)`                                                                                                          | A registry view in which only `names` exist.                                                                                                                                                                                                                                                                                                                                                              |
| `wikiRead(profile, wiki)`, `wikiReadFlow`, `WikiReadInput`, `WikiReadOutput`, `WikiReadRefused`, `wikiReadName`, `defaultWikiMaxBytes` | The `wiki-read` flow: a granted relative file only, real-path confined, so a symlink to an ungranted page is refused.                                                                                                                                                                                                                                                                                     |
| `workspaceNotice(workdir)`, `WorkspaceTools`, `Options`, `Built`                                                                       | The teaching a workspace role gets, and the models.                                                                                                                                                                                                                                                                                                                                                       |

| `webFetch(profile, retrieval, log)`, `webFetchFlow`, `WebFetchInput`, `WebFetchOutput`, `WebFetchRefused`, `webFetchName` | The `web-fetch` flow, run on the host: one public `http`/`https` page inside `grants.retrieval`, no address literals, every resolved address public and the connection pinned to it, each redirect re-checked, 20 s, 2 MiB read, 60,000 characters, scripts and embedded content dropped, text inside an untrusted-data boundary. Each page is recorded in `log`. |
| `Retrieved`, `RetrievalLog` | One retrieved page (`requested`, `url`, `status`, `retrievedAt`) and where a task's are recorded. `Authority.layer` adds each to the result's evidence as `url` with its retrieval time; with `logDir` the record survives a restart. |
| `serverTools(profile)`, `retrievalNotice` | The provider's web search for a `retrieval` holder, restricted to `allow`, none when `deny` is set, offered only with `retrieval.providerSearch`; and the teaching a retrieval role gets. |

`wiki-write` and `delegate` grants bind no flow yet.

## `@smthrs/organization/Workspace`

| Export                                                                                                                                                                                                                                                                                           | Behavior                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `microsandbox(options)`                                                                                                                                                                                                                                                                          | `Machines` over local Microsandbox microVMs: sticky workspace machines, ephemeral fresh ones, forced removal by remote id, and prepared bases as disk snapshots (`smthrs-env-<owner>-<name>`, the newest two per repository kept). A boot names a base and a `Network` (`none`, `all`, or a domain allowlist); without one, networking is off unless `network: true`. Image boots get a `diskMib` root disk (default `defaultDiskMib`, 32 GiB, sparse). The provider refuses a hosted backend. Workspace machines carry their key in the `workspaceLabel` (`smithers.workspace`) label. A machine whose guest dies is started again on its disk; `flush` (`sync`) writes a seeded workspace to that disk.                                                                                                                                                                                             |
| `layer({ machines, maxConcurrentVMs, environments?, limits?, identity? })`                                                                                                                                                                                                                       | The `Workspace` service; a semaphore bounds machine operations. `environments` maps a repository's host path to its `Environment`. Requires `ChildProcessSpawner`, `FileSystem`, and `Path`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `prepare({ key, repoPath, commit })`                                                                                                                                                                                                                                                             | Seeds `/workspace` from `git archive` of the resolved commit and records a guest baseline commit. Reuses a workspace seeded at the same commit; refuses one seeded at another (`occupied`). With an environment `prepare`, the workspace boots from the repository's prepared base instead (made first when missing: seeded, the command run with its network, the disk captured; `prepare-failed` carries the command's last output) and the source difference to the commit is synced onto it, falling back to the archive when it does not apply. The base is keyed by the command, its network, the key paths' content at the commit, and the machines' identity. The prepared tree is flushed before the capture; a base is booted once per host before use to confirm it holds the tree, and one that does not is removed and prepared again. Pruning keeps bases a task is about to boot from. |
| `resolveBase({ repoPath, commit? })`                                                                                                                                                                                                                                                             | The commit a task starts from: `commit`, where `HEAD` (the default) names the environment's `base`; a `<remote>/<branch>` of a configured remote is fetched first within `fetchTimeoutMs` (`fetch-failed` otherwise). Only that remote-tracking ref moves.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `findTools({ key, repoPath, commit })`                                                                                                                                                                                                                                                           | Boots a machine from the prepared base (preparing it when missing) and finds each command in `prepare.tools`: `{ base, tools: [{ name, path }] }`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `session(key, { commit? })`                                                                                                                                                                                                                                                                      | Reattaches the workspace machine for a scope, holding a machine permit. Refuses a machine holding no seeded workspace (`unseeded`: lost with its host, or never prepared) and one seeded from another commit than `commit` (`occupied`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `collect(prepared)`                                                                                                                                                                                                                                                                              | The binary-safe diff against the baseline and per-file statistics; `too-large` above `limits.diffBytes`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `runChecks({ key, repoPath, commit, patch, checks })`                                                                                                                                                                                                                                            | Seeds a fresh machine (from the prepared base when there is one), applies the patch, runs the environment's checks and then each `Check { name, argv, timeoutMs? }`, and returns receipts with bounded stdout and stderr (`truncated` and full `bytes` are recorded). `passed` only when every check exits 0. A check machine that goes away is replaced once and the checks start over.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `dispose(prepared)`                                                                                                                                                                                                                                                                              | Removes the workspace machine.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `applyChange({ repoPath, branch, parent, patch, message, runId, principal, at })`                                                                                                                                                                                                                | Lands the patch on `branch` with a temporary index and `commit-tree`, `Smithers-Run` and `Smithers-Principal` trailers, and a compare-and-set ref update against `parent`. Never pushes, never touches the working tree or index, refuses a checked-out branch (`checked-out`) and a moved branch (`moved-parent`). Landing the same request twice yields the same commit.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `Environment`, `Prepare`, `Network`, `Domain`, `KeyPath`, `Tool`, `BaseRef`, `ResolvedBase`, `ToolsFound`, `fetchTimeoutMs`, `basePrefix(owner)`, `Boot`, `Bases`, `maxPrepareTimeoutMs`, `defaultPrepareTimeoutMs`, `defaultDiskMib`                                                            | A repository's environment (`base`, `prepare { run, key, network, timeoutMs?, tools? }`, `network`, `checks`; a base without one of `tools` is not captured), how machines boot, and prepared bases.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `Prepared`, `Diff`, `FileStat`, `Check`, `CheckReceipt`, `Checks`, `Output`, `Applied`, `Key`, `CommitId`, `Machines`, `Limits`, `defaultLimits`, `Identity`, `defaultIdentity`, `WorkspaceError`, `WorkspaceErrorCode`, `parseNumstat`, `maxCheckTimeoutMs`, `SessionOptions`, `workspaceLabel` | Models, errors, and constants.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

## `@smthrs/organization/Actions`

| Export                                                                                                                                       | Behavior                                                                                                                                                                                                                                                                                                              |
| -------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `PinRoster`                                                                                                                                  | Records the registry's current revision (nondeterministic, so replay reads the recorded one).                                                                                                                                                                                                                         |
| `ComposeTask`                                                                                                                                | Resolves a principal at a revision and returns a ready `RoleTaskPayload`, or `DispatchRefused`.                                                                                                                                                                                                                       |
| `RoleTask`                                                                                                                                   | The `AgentAction` returning `Profile.RoleResult`; install `Authority.layer(RoleTask.layer)`.                                                                                                                                                                                                                          |
| `ValidateResult`, `Validation`                                                                                                               | A result's charter check at a pinned revision.                                                                                                                                                                                                                                                                        |
| `ResolveBase`, `PrepareWorkspace`, `CollectDiff`, `RunChecks`, `DisposeWorkspace`, `ApplyChange`, `ApplyPayload`, `unchanged`, `changeCheck` | Workspace steps over named repositories. `RunChecks` of an empty change boots nothing and fails one `change` receipt (`unchanged(commit)`). The workspace key is `<execution>/<repository>/<slug>`, and `executionOfWorkspace(key)` reads its execution back; `ApplyChange` is irreversible and keyed by its content. |
| `WriteReceipt`, `ReceiptName`, `ReceiptFailed`, `runDirectory`                                                                               | Writes `<generatedDir>/<run>/<name>.json` atomically, confined to the wiki root.                                                                                                                                                                                                                                      |
| `layer({ repositories, wiki: { root, generatedDir } })`                                                                                      | Implements every declaration but `RoleTask`. Requires `RosterRegistry`, `Workspace`, `FileSystem`, `Path`.                                                                                                                                                                                                            |
| `reviewHandler({ verdictField? })`                                                                                                           | A `GatesLive.ReviewHandler` running the reviewer's role task on the gate subject; approve only for a charter-valid `done` result whose `verdict` field is `approve`.                                                                                                                                                  |
| `Repository`, `Slug`, `Options`, `ReviewOptions`, `Requirements`                                                                             | Models.                                                                                                                                                                                                                                                                                                               |
