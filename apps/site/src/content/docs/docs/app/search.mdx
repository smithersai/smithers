---
title: "Search the app"
description: "Use the command palette to find files, flows, notes, and other project information."
sidebar:
  order: 4
---

import AppScreenshot from "../../../../components/AppScreenshot.astro"

Use the search palette when you know what you want but do not know which card or command opens it.

## Open search

1. Open **Chat** with **Command+K** on macOS or **Ctrl+K** on Windows and Linux.
2. Type `?` to choose a search type, then enter a name or part of a name.
3. Select a result or use the arrow keys and **Enter**.

The composer accepts ordinary chat as well as searches. Search prefixes select files, flows, and other result types; typing `/` opens the flow and command menu. Press **Escape** to dismiss the current menu before closing Chat.

<AppScreenshot src="/images/app/search.png" alt="The Smithers search palette lists search types and their prefixes after entering a question mark." caption="Type ? in the palette to inspect the search types and their prefixes." />

## Find a command

Type `/review` to find the repository's review flow. Select the flow to invoke it. To browse the Review group's actions, type `/review.` instead.

<AppScreenshot src="/images/app/slash.png" alt="Searching for /review shows a flow result and a separate Review command group." caption="A bare flow name runs a flow; a trailing dot explores its command group." />

## Search a specific area

The palette offers search types for files, flows, targets, wiki pages, history, runs, changes, issues, boxes, and secret names. Choose the relevant type to narrow the query. Secret search concerns names and metadata, not secret values.

Available results depend on your sign-in state, selected repository, and connected box. If an area says it is unavailable, follow the reason shown in the app. An unavailable search is different from a search that ran and found no matches.

For a command that requires additional fields, Smithers opens a form in the conversation. Fill in the fields and review the action before submitting it.
