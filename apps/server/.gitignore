# Deploy receipts (generated by scripts/deploy.ts) — regenerated per run, never commit.
deploy-receipts/

# Left behind by the retired Alchemy deploy path (2026-09-10 to 2026-09-12). A
# state store under it holds every secret that deploy read, in cleartext, so
# it stays ignored until the directory is gone from every checkout.
.alchemy/
