---
title: Model Selection
description: Folded into Recipes.
---

This material is now in [Recipes](/recipes).

Rule-of-thumb assignments: Codex (`gpt-5.3-codex`) for implementation, validation, and review-fix; Claude Opus (`claude-opus-4-6`) for research, planning, review, and reports; Claude Sonnet for cheap tool calls. Run two different models in parallel for review — they have different blind spots. Codex `model_reasoning_effort` defaults to `high`; bump to `xhigh` for multi-file refactors.
