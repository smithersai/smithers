---
title: Troubleshooting
description: Folded into the CLI overview.
---

This material is now in [CLI Overview](/cli/overview).

When a CLI agent fails, Smithers automatically runs diagnostics (binary installed, API key valid, rate limit hit) and prints a one-line summary to stderr; the full report is attached to the error's `details.diagnostics` field.
