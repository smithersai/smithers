# MVP release verification — 2026-09-16

This is a release evidence record, not an assertion that every release gate has passed. The signed-in issue setup chain and exact-source native PR review/CI have passed. Feature landing exposed a source-selection defect; release gates remain open.

The current release state is the [2026-09-19 section](#release-state-as-of-2026-09-19-1700-utc) below. Everything from "Deployed release as of 2026-09-17 03:40 UTC" onward is the 2026-09-16/17 record, kept as written. **Every claim in it about a deployed identity, about a coding-host pin, and about whether a workspace has been repinned is superseded by the 2026-09-19 section above.** In particular, its "No workspace repin is claimed from the newer API observation" (the Native coding host row) recorded the state on 2026-09-17; two coding-host repins have happened since, and the pinned host is now `01db3ae7…227b` built from `f6b42949c705`.

## Release state as of 2026-09-19 17:00 UTC

As-of stamp: 2026-09-19T17:00:13Z (`[D1]`), the moment `main@origin` and the deploy log were read. **No live probe was made at this stamp:** the deployed web identity below comes from the autodeploy log, not from a `https://smithers.sh/__build.json` read, so it is a deploy record rather than a served response. Receipts written after that stamp are not in this section.

**How to read this section.** Five earlier passes each asserted a derived number in prose and each introduced a number its own stated derivation did not produce. So the form changed: **every number in this section carries a tag into the block below, and the block holds the command that produced it and that command's real output, pasted.** Where a thing is a judgement and not a measurement it is stated in words with no figure. Nothing here is a present-tense claim about a system this lane did not read: `main@origin` and the deploy log both move by the minute, so each is stamped at `[D1]` and re-checked by re-running its own command, not by trusting this sentence.

### The numbers, with the command that produces each

Run these from `/Users/williamcory/smithers`. Every `jj` call carries `--ignore-working-copy`: it reads a
revision and never a working copy. Outputs are pasted as they came back at `[D1]`.

```
[D1] the as-of stamp
$ date -u +%Y-%m-%dT%H:%M:%SZ
2026-09-19T17:00:13Z

[D2] main@origin at the stamp
$ jj --ignore-working-copy log --no-graph -r 'main@origin' -T 'commit_id ++ "\n"'
0068f10c2b350aab4647bbb0737cd6f7083f1bf9

[D3] the last web deploy the driver recorded, at the stamp
$ grep -n 'smithers: deployed' ~/.config/smithers-autodeploy/autodeploy.log | tail -1
3670:20260919T162007.828933Z smithers: deployed 0068f10c2b350aab4647bbb0737cd6f7083f1bf9
  Re-run after the stamp and this returns a later line: the driver deploys every few minutes.
  That is the point of pasting it. The web deploys between the 03:14Z stamp and this one are
  the bounded range, which does not move:
$ awk 'NR>3531 && NR<=3670 && /smithers: deployed/ {print NR": "$0}' ~/.config/smithers-autodeploy/autodeploy.log
3542: 20260919T032801.515240Z smithers: deployed 44545559b4bd59583bb1b876b342dacb5b9ded21
3569: 20260919T040429.538811Z smithers: deployed d103ee7eaef251f60787804ff2ee02f46e90586e
3572: 20260919T040623.938669Z smithers: deployed ed58abfcebdf0a3f6443fca50b657434898b3161
3581: 20260919T081436.291102Z smithers: deployed a98f757e71c0301c463adbd6c617c5bbf9d498a8
3586: 20260919T082753.124744Z smithers: deployed effcc3d77f4e2f30997e9b4c792615967b31d214
3665: 20260919T161931.107890Z smithers: deployed f175fbf15dcbfe878df76202d7da311845d18bf4
3670: 20260919T162007.828933Z smithers: deployed 0068f10c2b350aab4647bbb0737cd6f7083f1bf9
$ for s in 44545559b4bd d103ee7eaef2 ed58abfcebdf a98f757e71c0 effcc3d77f4e f175fbf15dcb 0068f10c2b35; do
    printf '%s [%s]\n' "$s" "$(jj --ignore-working-copy log --no-graph -r "$s & ::0068f10c2b35" -T 'commit_id.short(12)')"
  done
44545559b4bd [44545559b4bd]
d103ee7eaef2 [d103ee7eaef2]
ed58abfcebdf [ed58abfcebdf]
a98f757e71c0 [a98f757e71c0]
effcc3d77f4e [effcc3d77f4e]
f175fbf15dcb [f175fbf15dcb]
0068f10c2b35 [0068f10c2b35]

[D4] the last Plue deploy the driver recorded, at the stamp
$ grep -n 'plue: deployed' ~/.config/smithers-autodeploy/autodeploy.log | tail -1
2773:20260918T084507.215471Z plue: deployed c0bd4c8218c3d6ac99e031cb16a8049e9e187903

[D5] commits between the web build the older section records and the stamped sha
$ jj --ignore-working-copy log --no-graph -r '4ca45c0b2d2e..0068f10c2b35' -T 'commit_id.short(12) ++ "\n"' | wc -l
     374

[D6] commits between the pinned host's source and the stamped sha
$ jj --ignore-working-copy log --no-graph -r 'f6b42949c705..0068f10c2b35' -T 'commit_id.short(12) ++ "\n"' | wc -l
     170

[D7] of those, the ones that touch flows/
$ jj --ignore-working-copy log --no-graph -r 'f6b42949c705..0068f10c2b35 & files("flows")' -T 'commit_id.short(12) ++ "\n"'
42b8abbc1cf6
e9eb2412c039
1e2cf70541a8
7c0647793d5c
e12df9d7dd96
a19cb0ae9240
e8ae1a1d60b3
51cac5dd8b2a
8af5ed0d90d5
243e5f4b8d40
8a28a17f43b9
2a9c14aa1307
3638d4ef09fa
31aa4863f92a
d5e1d8093491
e012f09bced8
f55c2f725ee0
211f4fde1e2b
$ … | wc -l
      18

[D8] flows/ paths per commit: every flows/ path, then the ones outside flows/test/
$ for s in $(jj --ignore-working-copy log --no-graph -r 'f6b42949c705..0068f10c2b35 & files("flows")' -T 'commit_id.short(12) ++ "\n"'); do
    p=$(jj --ignore-working-copy diff -r "$s" --name-only | grep -c '^flows/')
    n=$(jj --ignore-working-copy diff -r "$s" --name-only | grep '^flows/' | grep -vc '^flows/test/')
    printf '%s all=%s nontest=%s\n' "$s" "$p" "$n"
  done
42b8abbc1cf6 all=10 nontest=8
e9eb2412c039 all=6 nontest=3
1e2cf70541a8 all=7 nontest=4
7c0647793d5c all=7 nontest=3
e12df9d7dd96 all=4 nontest=1
a19cb0ae9240 all=6 nontest=0
e8ae1a1d60b3 all=1 nontest=0
51cac5dd8b2a all=9 nontest=3
8af5ed0d90d5 all=14 nontest=8
243e5f4b8d40 all=10 nontest=6
8a28a17f43b9 all=12 nontest=8
2a9c14aa1307 all=5 nontest=4
3638d4ef09fa all=8 nontest=5
31aa4863f92a all=3 nontest=2
d5e1d8093491 all=2 nontest=1
e012f09bced8 all=7 nontest=6
f55c2f725ee0 all=4 nontest=3
211f4fde1e2b all=3 nontest=1
$ … | awk -F'nontest=' '$2 > 0' | wc -l
      16

[D9] landing receipts inside this section's window
$ ls .artifacts/mvp-release-20260916/app-landing-2026091[89]T*.json | wc -l
      26

[D10] of the 170 at [D6], how many any receipt names — at both prefix widths, because the answer differs
$ jj --ignore-working-copy log --no-graph -r 'f6b42949c705..0068f10c2b35' -T 'commit_id.short(8) ++ "\n"' \
  | while read s; do grep -lq "$s" .artifacts/mvp-release-20260916/app-landing-2026091[89]T*.json && echo named || echo unnamed; done \
  | sort | uniq -c
  50 named
 120 unnamed
$ jj --ignore-working-copy log --no-graph -r 'f6b42949c705..0068f10c2b35' -T 'commit_id.short(12) ++ "\n"' \
  | while read s; do grep -lq "$s" .artifacts/mvp-release-20260916/app-landing-2026091[89]T*.json && echo named || echo unnamed; done \
  | sort | uniq -c
  47 named
 123 unnamed
$ for p in ae9d711a 2823679e fb0512fa; do grep -ln "$p" .artifacts/mvp-release-20260916/app-landing-2026091[89]T*.json; done
.artifacts/mvp-release-20260916/app-landing-20260918T2245Z.json
.artifacts/mvp-release-20260916/app-landing-20260918T1603Z.json
.artifacts/mvp-release-20260916/app-landing-20260918T1450Z.json
$ grep -h 'ae9d711a' .artifacts/mvp-release-20260916/app-landing-20260918T2245Z.json
  "tests": "combined tree on ae9d711a: app 4 files 135 pass + check exit 0; rpc RepositorySetup 37 pass; server 79 pass"

[D11] each of the 18 at [D7] against the 26 receipts at [D9]
$ for s in $(jj --ignore-working-copy log --no-graph -r 'f6b42949c705..0068f10c2b35 & files("flows")' -T 'commit_id.short(12) ++ "\n"'); do
    printf '%s %s\n' "$s" "$(grep -l "$s" .artifacts/mvp-release-20260916/app-landing-2026091[89]T*.json | xargs -n1 basename | tr '\n' ' ')"
  done
42b8abbc1cf6
e9eb2412c039 app-landing-20260918T2305Z.json
1e2cf70541a8 app-landing-20260918T2305Z.json
7c0647793d5c
e12df9d7dd96
a19cb0ae9240
e8ae1a1d60b3
51cac5dd8b2a
8af5ed0d90d5 app-landing-20260918T2150Z.json
243e5f4b8d40
8a28a17f43b9
2a9c14aa1307
3638d4ef09fa
31aa4863f92a app-landing-20260918T1627Z.json
d5e1d8093491 app-landing-20260918T1603Z.json
e012f09bced8 app-landing-20260918T1512Z.json
f55c2f725ee0
211f4fde1e2b app-landing-20260918T1456Z.json
$ grep -n '8af5ed0d90d5' .artifacts/mvp-release-20260916/app-landing-20260918T2150Z.json
4:  "from": "8af5ed0d90d5",
$ grep -n 'e012f09bced8' .artifacts/mvp-release-20260916/app-landing-20260918T1512Z.json
4:  "from": "e012f09bced8",

[D12] the gap between the 23:05Z receipt's `to` and the 01:10Z receipt's `from`, and which of it no receipt names
$ jj --ignore-working-copy log --no-graph -r 'e9eb2412c039..1917b94fef46' -T 'commit_id.short(12) ++ "\n"'
1917b94fef46
57a48653c7e8
270f7864e9ce
42b8abbc1cf6
ef9537884502
3d1bb8876535
a71b5853fad0
$ for s in $(jj --ignore-working-copy log --no-graph -r 'e9eb2412c039..1917b94fef46' -T 'commit_id.short(12) ++ "\n"'); do
    grep -lq "$s" .artifacts/mvp-release-20260916/app-landing-2026091[89]T*.json || echo "$s"; done
270f7864e9ce
42b8abbc1cf6
ef9537884502
3d1bb8876535
a71b5853fad0

[D13] where the other two of that seven are named
$ grep -n '1917b94fef46\|57a48653c7e8' .artifacts/mvp-release-20260916/app-landing-20260919T0110Z.json
6:  "from": "1917b94fef46",
19:    "cause": "main 57a48653c7e8 (local-backend retirement, 261 files, -31435 lines) removed the connector.add door …
$ grep -n '1917b94fef46\|57a48653c7e8' .artifacts/mvp-release-20260916/app-landing-20260919T0140Z.json
31:  "mainIsRed": "… Introduced by the local-backend retirement (57a48653c7e8, 1917b94fef46, b6d6ece7a378). …"

[D14] W1's verdict table (W = .artifacts/mvp-canary-walk-20260917/W1-REPORT.md)
$ grep -c '^| [0-9]' "$W"
14
$ grep '^| [0-9]' "$W" | grep VERIFIED | grep -cv 'OPEN'
9
$ grep '^| [0-9]' "$W" | grep VERIFIED | grep -c 'OPEN'
1
$ grep '^| [0-9]' "$W" | grep -v VERIFIED | grep -c 'OPEN'
2
$ grep '^| [0-9]' "$W" | grep -c 'PARTIAL'
2

[D15] W1's own "Still broken on the web half" list: the heading, then the numbered items
$ grep -n '^## Still broken on the web half' "$W"
37:## Still broken on the web half
$ sed -n '39,45p' "$W" | grep -c '^[0-9]\.'
7

[D16] the walk audit's run-2 defect list (A = .artifacts/mvp-claude-orchestration-20260917/AUDIT-canary-walk-20260917.md)
$ sed -n '202,225p' "$A" | grep -c '^| [A-Z]-'
24
$ sed -n '202,225p' "$A" | grep '| \*\*CLOSED' | grep -cv 'OPEN\|Still present\|first half'
9
$ sed -n '202,225p' "$A" | grep '| \*\*CLOSED' | grep -c 'OPEN\|Still present\|first half'
3
$ sed -n '183,200p' "$A" | grep '^| [0-9]' | grep -c '\*\*CLOSED'
1

[D17] the five typecheck positions L105's receipt recorded, read at the stamped sha
$ jj --ignore-working-copy file list -r 0068f10c2b35 apps/app/src | grep -c 'AppServices.ts'
0
$ for f in apps/app/src/mainview/cards/FileCards.test.tsx apps/app/src/mainview/ChromeDock.test.tsx; do
    jj --ignore-working-copy file show -r 0068f10c2b35 "$f" | grep -c socketUrl; done
0
0
$ jj --ignore-working-copy file show -r 0068f10c2b35 apps/app/src/mainview/state/controller/presentation.ts | grep -c '"experimental"'
0
$ jj --ignore-working-copy log --no-graph -r '81bca45092e9..0068f10c2b35 & (files("apps/app/src/mainview/ChromeDock.test.tsx") | files("apps/app/src/mainview/state/controller/presentation.ts"))' -T 'commit_id.short(12) ++ " " ++ description.first_line() ++ "\n"'
f175fbf15dcb 🐛 fix(app): the app stops naming a hidden-mock surface it does not carry
8b2b72523730 🐛 fix(app): two harnesses stop binding a socket URL the app no longer takes

[D18] the sandbox capacity guard this section recorded at the 03:14Z stamp
$ ps -o pid= -p 77296
$ echo $?
1

[D19] flows/ commits landed between the 03:14Z stamp's sha and this one
$ jj --ignore-working-copy log --no-graph -r '81bca45092e9..0068f10c2b35 & files("flows")' -T 'commit_id.short(12) ++ "\n"' | wc -l
       0

[D20] the six commits below, against the stamped sha and against the pinned host's source
$ for s in 211f4fde1e2b d5e1d8093491 31aa4863f92a 369fa2965c6d 50479793be89 c8b777f7993e; do
    a=$(jj --ignore-working-copy log --no-graph -r "$s & ::0068f10c2b35" -T 'commit_id.short(12)')
    b=$(jj --ignore-working-copy log --no-graph -r "$s & ::f6b42949c705" -T 'commit_id.short(12)')
    printf '%s in-stamped=[%s] in-pin=[%s]\n' "$s" "$a" "$b"
  done
211f4fde1e2b in-stamped=[211f4fde1e2b] in-pin=[]
d5e1d8093491 in-stamped=[d5e1d8093491] in-pin=[]
31aa4863f92a in-stamped=[31aa4863f92a] in-pin=[]
369fa2965c6d in-stamped=[369fa2965c6d] in-pin=[]
50479793be89 in-stamped=[50479793be89] in-pin=[]
c8b777f7993e in-stamped=[c8b777f7993e] in-pin=[]

[D21] every source line this section cites, read at the stamped sha
$ jj --ignore-working-copy file show -r 0068f10c2b35 <file> | sed -n '<lines>p'
flows/coding/host.ts:171
    const evaluator = evaluatorLayer(process.env)
packages/smithers/src/internal/NativeControl.ts:606
    const evaluator = Evaluator.layerFromEnvironment(environment).pipe(Layer.provide(native.httpClient))
packages/smithers/agent/model/src/Evaluator.ts:318,321
export interface Request {
  readonly state: unknown
  readonly questions: Readonly<Record<string, Question>>
}
packages/smithers/agent/model/src/Classifier.ts:475,477
    Effect.forEach(states, (value) => Effect.result(evaluate(value)), {
      concurrency: all.concurrency ?? defaultConcurrency
    })
apps/server/src/jevRelay.ts:59
export const JEV_QUESTIONS_MAX = 8
apps/server/src/jevRelay.ts:213,216
    const own = yield* limits.spend(key, RECOMMEND_CEILING)
    if (!own.allowed) return turnLimitResponse(own, headers, RECOMMEND_CEILING)
    const shared = yield* limits.spend(RECOMMEND_ALL_KEY, RECOMMEND_ALL_CEILING)
    if (!shared.allowed) return turnLimitResponse(shared, headers, RECOMMEND_ALL_CEILING)
apps/server/src/recommend.ts:124
export const RECOMMEND_ADDRESS_MAX = 300
apps/server/src/recommend.ts:126
export const RECOMMEND_ALL_MAX = 5000
apps/server/src/recommend.ts:777,780
export const recommendKey = (request: Request, login: string | undefined, salt: string | undefined): Effect.Effect<string> =>
  login === undefined
    ? Effect.map(anonymousTurnKey(request, salt), (key) => `recommend:${key}`)
    : Effect.succeed(`recommend:login:${login}`)
apps/server/src/index.ts:340
      const validation = request.headers.has("cookie") ? yield* validateSession(request) : undefined
flows/repository/intake.ts:68,77,87
    injection: Classifier.boolean({
    kind: Classifier.choice({
    urgency: Classifier.score({

[D22] L97b's three candidate commits against the stamped sha
$ for s in 66244a19ce42 0a6efd139059 1cce989ae071; do
    printf '%s [%s]\n' "$s" "$(jj --ignore-working-copy log --no-graph -r "$s & ::0068f10c2b35" -T 'commit_id.short(12)')"
  done
66244a19ce42 []
0a6efd139059 []
1cce989ae071 []

[D23] L87's two commits against the pinned host's source; the three web commits' flows/ paths
$ for s in 5e43afbe547d fe4bc28d8cd5; do
    printf '%s [%s]\n' "$s" "$(jj --ignore-working-copy log --no-graph -r "$s & ::f6b42949c705" -T 'commit_id.short(12)')"
  done
5e43afbe547d [5e43afbe547d]
fe4bc28d8cd5 [fe4bc28d8cd5]
$ for s in 369fa2965c6d 50479793be89 c8b777f7993e; do
    printf '%s flows=%s\n' "$s" "$(jj --ignore-working-copy diff -r "$s" --name-only | grep -c '^flows/')"
  done
369fa2965c6d flows=0
50479793be89 flows=0
c8b777f7993e flows=0

[D24] the L87 Plue guard, rebased: same tree, and only the rebased sha is in the deployed tag (run from ~/plue)
$ jj --ignore-working-copy diff --from 982372d7a903 --to 772414e604b6 --stat
0 files changed, 0 insertions(+), 0 deletions(-)
$ printf '772414e604b6 [%s]\n982372d7a903 [%s]\n' \
    "$(jj --ignore-working-copy log --no-graph -r '772414e604b6 & ::c0bd4c8218c3' -T 'commit_id.short(12)')" \
    "$(jj --ignore-working-copy log --no-graph -r '982372d7a903 & ::c0bd4c8218c3' -T 'commit_id.short(12)')"
772414e604b6 [772414e604b6]
982372d7a903 []

[D25] questions declared per classifier under flows/, the relay's state cap, and the pinned host's own source
$ for f in $(jj --ignore-working-copy file list -r 0068f10c2b35 flows | grep '\.ts$'); do
    n=$(jj --ignore-working-copy file show -r 0068f10c2b35 "$f" | grep -c 'Classifier\.\(boolean\|choice\|score\)(')
    [ "$n" -gt 0 ] && printf '%s %s\n' "$f" "$n"
  done | sort -k2 -rn
flows/repository/intake.ts 3
flows/wiki/jev-citations.ts 1
flows/repository/jev-score.ts 1
flows/repository/jev-reproduction.ts 1
flows/repository/jev-duplicates.ts 1
flows/repository/jev-checks.ts 1
flows/release-content/jev-template.ts 1
$ jj --ignore-working-copy file show -r 0068f10c2b35 flows/repository/jev-checks.ts | grep -n 'export const MAX_STATES'
35:export const MAX_STATES = 64
$ jj --ignore-working-copy file show -r f6b42949c705 flows/coding/host.ts | grep -c evaluatorLayer
0
$ jj --ignore-working-copy file show -r f6b42949c705 packages/smithers/src/internal/NativeControl.ts | grep -c layerFromEnvironment
0
```

| Component | Identity now | How it was checked |
| --- | --- | --- |
| Web / app | Smithers `0068f10c2b350aab4647bbb0737cd6f7083f1bf9` ("docs(app): why a model credential is a name pinned to its origins"), the last deploy the driver recorded at `[D1]` | The deploy record is `[D3]`; `main@origin` is `[D2]`. The two are the same sha at `[D1]`, which is a fact about that moment and not a property of either: seven web deploys landed between the 03:14Z stamp and this one, each one a sha `main@origin` had already passed (`[D3]`, second command). Re-read `[D2]` and `[D3]` rather than this sentence; by the time this is read the driver will have deployed again. The deploy driver's own record is all that is claimed: the 2026-09-18 stamp's `42b8abbc1cf6` was confirmed by a live `__build.json` read, this one is not, so "deployed" means the driver reported success, not that a served response was read back. `[D5]` is how far this build is from the web build the older section records. Every count in this section is taken against `0068f10c2b35`, the sha `[D2]` returned, not against a live `main@origin`. The five typecheck errors the 03:14Z stamp recorded are no longer at their positions in this tree — `[D17]`, and the "`main` does not typecheck" gate below for what that does and does not settle. |
| Plue API | Tag `c0bd4c8218c3`, Helm release `smithers` revision 435 | The deploy record is `[D4]`, and it is still the last Plue deploy at `[D1]`. Driver log `~/.config/smithers-autodeploy/logs/plue-20260918T075227.786738Z.log`: `:8144` `STATUS: deployed`, `:8145` `REVISION: 435`, `:8138` `All 7 required workload images confirmed at tag c0bd4c8218c3`, `:8153` `Release deep canary smithers-release-deep-4144f3e6 passed`, `:8154` `Post-deploy canary gate passed`, `:8159` `Deploy complete!`. Receipt `plue-rollout-20260918-repin2.json` (base `c26f08208acf`, commits `772414e6` and `c0bd4c82`). |
| Native coding host, in the API image | sha256 `01db3ae783878d02231257427092ad086229337e4e47d98eb3db907ac951227b`, policy digest `aea344b88d93822684e759d3ffd21628a2d5845b3e6a3f886b0ad25730f69fda`, built from Smithers `f6b42949c705fc806938f332e1a322eca45311f5` | Receipt `host-landing-and-pin-20260918T0800Z.json` (`hostArtifact`), landing receipt `app-landing-20260918T0758Z.json` for `f6b42949` itself. `jj --ignore-working-copy log --no-graph -r 'f6b42949c705 & ::0068f10c2b35' -T 'commit_id.short(12)'` returns `f6b42949c705`, so the pinned source is an ancestor of the stamped sha; the revset is written against `[D2]`'s sha rather than against a live `main@origin`, which moves. The pod read (`smithers-api-5c968656cc-lvhwm`, `sha256sum /usr/local/bin/smithers-coding-host` = `01db3ae7…`, 2026-09-18T10:12Z) is the release owner's own `kubectl exec`, recorded in `plue-rollout-20260918-repin2.json` `hostInPod` and `EXPECTED.json` `run3.apiImageVerified`. This lane made no cluster request. **Stated as a limitation, not closed:** those two receipts have one origin, that single command, and it is the only link between "the API image is tagged `c0bd4c8218c3`" and "the running pod holds host `01db3ae7…`". No document in this repository can check it; a reader with cluster access can, in one `kubectl exec`. |
| Coding host, in every workspace VM | Not measured by this lane | The walk's receipts are the only VM-side evidence and they belong to run 3, below. |

### What landed in rounds 3 and 4

26 landing receipts fall inside this section's window (`[D9]`), the last two written on 2026-09-19 — `…0110Z.json` (L104, `from` `1917b94fef46`, `to` `7a854f57481d`, `half` = `WEB …`, zero `flows/` paths) and `…0140Z.json` (L103 + L103b, `from` `b6d6ece7a378`, `to` `e35dcbfcfa17`, `half` = `WEB …`, zero `flows/` paths). Eleven of them cover lanes behind the web fixes W1 verified live — not every such lane, as the next sentence shows: L74/L74b/L75/L76 (`app-landing-20260918T0053Z.json`), L77/L77b (`…0149Z.json`), L82 (`…0731Z.json`), L83 (`…0701Z.json`), L88/L88b (`…1420Z.json`), L91 (`…1424Z.json`), L92c (`…1450Z.json`), L97 `50479793be89` (`…1512Z.json`), L98 app half `ad5757502039` (`…1627Z.json`), L98b `369fa2965c6d` (`…1951Z.json`), L100 `c8b777f7993e` (`…2150Z.json`) — eleven receipt files, countable in that list. That is not every lane W1 credits: W1 row 3a is credited to `L91/L99` and row 6 to `L76/L96c` (`.artifacts/mvp-canary-walk-20260917/W1-REPORT.md:25,35`), and neither L99 nor L96c is in the list above. L99 is named in no receipt at all (`grep -l L99 .artifacts/mvp-release-20260916/*.json`, exit 1); L96c's receipt is `app-landing-20260918T2245Z.json`, which the eleven-receipt list above does not include.

They also cover host-flow fixes, and two of those are **already in the pinned host**: L87 (`…0745Z.json`, commits `5e43afbe547d` and `fe4bc28d8cd5`) and L90 (`…0758Z.json`, commit `f6b42949c705`). `f6b42949c705` *is* the source the pinned host was built from, and both L87 commits are ancestors of it (`[D23]`). L87's receipt pairs its host half with a Plue commit — `half` = `HOST (flows/repository/activation.ts, checks.ts, remote.ts + tests; needs the D2 repin) — PAIRED with Plue 982372d7 (RegisterRepositoryJob equal-revision guard), which rides the pin release` — and `plue-rollout-20260918-repin2.json` lists `772414e6 (L87 Plue)` and `c0bd4c82`, not `982372d7`. The guard did ship, under a rebased sha: in `~/plue`, `982372d7a903` and `772414e604b6` carry the same first line ("fix(repository-jobs): a trial candidate re-registers at its own revision") and identical trees, and only the rebased sha is an ancestor of the deployed tag — both at `[D24]` — so the paired guard is in the deployed tag and the abandoned pre-rebase sha is not. Do not read either lane as blocked.

Every receipt from `03:52Z` onward states which half its commits are in a `half` field, and where that field names one half, it is what puts the commits it names on one side of the line below; where it reads `BOTH`, the `flows/` path test splits them. **The receipts are a partial record, authoritative only for the commits they name.** How partial depends on how a sha is matched, so both answers are stated. Grepping each of the 170 commits at `[D6]` against the 26 receipts at `[D9]`: by 8-character prefix, 50 named and 120 unnamed; by 12-character prefix, 47 named and 123 unnamed — both at `[D10]`. The three the widths disagree on are not prefix collisions but real 8-character mentions inside a receipt's `tests` prose, which the 12-character match discards: `ae9d711a2cbd` in `…2245Z.json`, `2823679e48df` in `…1603Z.json`, `fb0512fac750` in `…1450Z.json`. Neither number is "the" answer; the pair is the measurement, and the reader who wants one should pick the width first. Of the 18 `flows/` commits, seven are named by any receipt at either width and eleven by none (`[D11]`); of the seven, five are named as landed commits under a `commits[].id` — `211f4fde1e2b`, `d5e1d8093491`, `31aa4863f92a`, `1e2cf70541a8`, `e9eb2412c039` — and two appear only as another receipt's `from` base, which is not a landing (`8af5ed0d90d5` in `…2150Z.json`, `e012f09bced8` in `…1512Z.json`, both pasted at `[D11]`). Two receipts record their landings as a count string instead of a list of final shas, so even their own shas are unrecoverable from the receipt: `…0533Z.json` (`commits` = `12 (L80 chain rebased as L80c; originals 25c3a485..031b131d on 53d82707; rebased d01b6bd3..ed9ca208 on 836d3d31; duplicated onto c8af9fca)`) and `…2245Z.json` (`commits` = `3 (L96 trial fields outside the candidate + apply identity, rebuilt as L96c on current main)`, `originals` `3843a2a4`, `50c8d788`, `07c146cf`, and no `from` or `to` field at all). Every commit no receipt names is placed in this section by the `flows/` path test on `jj diff -r <sha> --name-only`, not by a `half` field. The two earliest, `…0053Z.json` (L74/L74b/L75/L76) and `…0149Z.json` (L77/L77b), carry no `half` field; all seven of their commits return zero `flows/` paths from `jj diff -r <sha> --name-only`, so they are web.

The three host-flow commits that answer walk run 3's blockers are **landed and not deployed**, each one a `flows/` change by its own landing receipt:

| Commit | Lane | Receipt | Receipt `half` | `flows/` paths in `jj diff -r <sha> --name-only`, `flows/test/` included |
| --- | --- | --- | --- | --- |
| `211f4fde1e2b` | L93 | `app-landing-20260918T1456Z.json` | `HOST (flows/repository/checks.ts; needs the third repin)` | 3 |
| `d5e1d8093491` | L94 | `app-landing-20260918T1603Z.json` | `HOST (flows/repository/delivery.ts; needs the third repin)` | 2 |
| `31aa4863f92a` | L98 | `app-landing-20260918T1627Z.json` | `BOTH (HOST flows/repository/schema.ts + triggers.ts …; WEB apps/app register form …)` — the HOST half is what waits; the WEB half is `ad5757502039` and is serving | 3 |

Three commits a reader may expect in that table are **web** commits and are ancestors of the stamped sha (`[D20]`), so they are in the build the driver last deployed at `[D3]`: `369fa2965c6d` (L98b, `app-landing-20260918T1951Z.json` `half` = `WEB (apps/app TriggersSeam …, apps/server repositoryTriggers, packages/rpc …)`), `50479793be89` (L97, `app-landing-20260918T1512Z.json` `half` = `WEB (apps/app SqliteRowStorage, AppProjection, AppStore, PersistenceBudget)`) and `c8b777f7993e` (L100, `app-landing-20260918T2150Z.json` `half` = `BOTH-web (apps/server repositorySetupExecution; apps/app SetupChecklist + turns)`). Each of the three returns zero `flows/` paths (`[D23]`). Do not read any of them as waiting on the repin.

All six are ancestors of the stamped sha and none is an ancestor of `f6b42949c705`, the source the pinned host was built from (`[D20]`) — but that pair of revsets separates nothing, because it is true of every one of the 170 commits landed since `f6b42949c705`, deployed and undeployed alike. The receipt `half` field and the `flows/` path test above are what place a commit. In total 170 commits separate `f6b42949c705` from the stamped sha (`[D6]`), 18 of which touch `flows/` (`[D7]`) — the same eighteen as at the 03:14Z stamp, because nothing landed since touches `flows/` (`[D19]`). The app-side commits among those 170 reach production through the web deploy; the `flows/` commits reach production only through a coding-host repin, and that repin is stopped.

**Those three rows are the blocker fixes, not the size of the repin.** **16 of the 18 `flows/` commits change `flows/` source outside `flows/test/`** — `[D8]`, where the per-commit `nontest=` column is 0 only for `a19cb0ae9240` and `e8ae1a1d60b3`. That column is also why the three-row table above reads 3, 2 and 3 for `211f4fde1e2b`, `d5e1d8093491` and `31aa4863f92a` while this paragraph would read 1, 1 and 2 for the same commits: the table's column is `all=` at `[D8]` and counts every `flows/` path, `flows/test/` included; this paragraph's is `nontest=`. All 16 wait on repin 3. **Seven** commits sit in the gap between the `23:05Z` receipt's `to` (`e9eb2412c039`, `app-landing-20260918T2305Z.json`) and the `01:10Z` receipt's `from` (`1917b94fef46`, `app-landing-20260919T0110Z.json`) — `[D12]` lists them — or six if `1917b94fef46` itself is read out of the gap as its endpoint. **Five** of those seven are the ones no receipt names at all, the second command at `[D12]`: `a71b5853fad0` (committed `2026-09-18T23:01:49Z`), `3d1bb8876535` (`23:15:34Z`), `ef9537884502` (`23:16:36Z`), `42b8abbc1cf6` (`23:16:36Z`) and `270f7864e9ce` (`2026-09-19T00:34:09Z`). The other two are named, and each appears in **both** 2026-09-19 receipts, never as a landing (`[D13]`): `1917b94fef46` as the `01:10Z` receipt's `from` base at its `:6` and inside the `01:40Z` receipt's `mainIsRed` prose at its `:31`; `57a48653c7e8` inside the `01:10Z` receipt's `cause` prose at its `:19` and inside that same `mainIsRed` line. An earlier pass of this section said `1917b94fef46` appeared only as a `from` base while granting `57a48653c7e8` the `mainIsRed` mention; `[D13]` is the command that refutes it, and the two shas have the same record. The seven is the size of the gap; the five is how many of it no receipt names. One of the five, `42b8abbc1cf6`, is in the 18 and is tied for the most non-test `flows/` files at 8 (`[D8]`); it has no landing receipt and no `half` field and is placed here only by the path test. Two of the 16 come from the `23:05Z` receipt, the last of the twenty-four written on 2026-09-18: `app-landing-20260918T2305Z.json` reads `half` = `BOTH (HOST flows/repository/{inspection,evaluation,setup,retention}.ts — needs repin 3; WEB apps/app controller + packages/rpc)` for `1e2cf70541a8`, `cb5587632fe1` and `e9eb2412c039`; at `[D8]` `1e2cf70541a8` is `nontest=4` and `e9eb2412c039` is `nontest=3`, and `cb5587632fe1` is not at `[D7]` at all, so it is web. The largest of the 16 are the ones that create the Jev dependency the repin is blocked on — at `[D8]`, sorted on `nontest=`: `8af5ed0d90d5` "Jev decides the observation, the reproduction verdict and the eval verdict" (8), `8a28a17f43b9` "Jev checks that every citation supports its claim" (8), `42b8abbc1cf6` (8), `e012f09bced8` "screen every inbound event with Jev" (6), `243e5f4b8d40` "Jev picks the release narrative" (6), `3638d4ef09fa` "AI checks and event intake are Jev only" (5) — so repin 3 is a sixteen-commit `flows/` change, not a three-commit one, and its blast radius is the reason the key question below exists. Neither this sixteen nor the eighteen in the Open-gates row below is that blast radius: the host is built from the whole tree at the repin source, so both counts say how much changed since the pin, not how much is rebuilt or re-exposed, and this section does not size the rebuild.

### The third coding-host repin is cut and stopped

A host built from today's `main` binds its Jev evaluator from `AI_GATEWAY_API_KEY` in its own process environment, in two independent places: `flows/coding/host.ts:171` (`const evaluator = evaluatorLayer(process.env)`) and `packages/smithers/src/internal/NativeControl.ts:606` (`Evaluator.layerFromEnvironment(environment)`), both re-read at the stamped sha and both still at those line numbers (`[D21]`). L101 measured the first at `:167` on `a71b5853`; the line moved with the round-4 landings and the binding did not. The name appears in no file in `~/plue` (`grep -rl AI_GATEWAY_API_KEY ~/plue`, exit 1), so no workspace VM has ever carried it, and the keyless bundle gate dies at the first completion inside `coding/implementation` before any check tier (`.artifacts/mvp-evaluator-reach-20260918/FINAL.md` §1; `red-bundle-plan-nokey.log`, exit 1; the same gate exits 0 once a judge answers, `green-bundle-plan.log`). L101's original cost comparison — that `apps/server/src/jevRelay.ts:59`'s `JEV_QUESTIONS_MAX = 8` collides with `flows/repository/jev-checks.ts:35`'s `MAX_STATES = 64` (`[D25]`) — was refuted by R101 (`R101-review-L101.md` §3) and is not repeated here: `Evaluator.Request` carries exactly one state (`packages/smithers/agent/model/src/Evaluator.ts:318-321`, `[D21]`), `Classifier.evaluateAll` already issues one request per state on the direct path (`Classifier.ts:475-477`, `[D21]`), and the largest question set any classifier under `flows/` declares is three, one boolean, one choice and one score in `flows/repository/intake.ts:68,77,87` — every other declaring file has one (`[D25]`, `[D21]`) — so an 80-hunk AI check is 80 one-question requests on either transport and the relay's question cap refuses none of them.

**The cost that survives that refutation is volume against a shared address ration, and it is what a relayed host would pay.** Before it asks Jev anything, `handleJev` spends one `RECOMMEND_CEILING` against the caller's `recommendKey` and one `RECOMMEND_ALL_CEILING` against the deployment — `apps/server/src/jevRelay.ts:213-216`, pasted at `[D21]`, both refusing the request when the bucket is empty. Those ceilings are the in-app recommender's own: `export const RECOMMEND_ADDRESS_MAX = 300` at `apps/server/src/recommend.ts:124`, documented at `:119-123` as "Recommendations one address, or one login, may ask for per day", and `export const RECOMMEND_ALL_MAX = 5000` at `:126`, "Recommendations the whole deployment may ask for per day" at `:125` — both at `[D21]`. That bucket is not a per-user ration. `recommendKey` falls back to the salted anonymous address digest whenever the caller has no login (`apps/server/src/recommend.ts:777-780`, `[D21]`), and the relay route reads a login only from a cookie (`apps/server/src/index.ts:340`, `[D21]`), which a workspace VM never sends — so every workspace behind one NAT egress address shares one 300-per-day bucket with the signed-out humans on that address. One relayed one-question request spends one of that shared 300. The rest of this arithmetic is quoted, not re-derived here — `sed -n '209,216p' .artifacts/mvp-evaluator-reach-20260918/FINAL.md` returns "80-hunk AI check spends **80 of 300**, or 27% of one address's day; four of them exhaust it" and "Deployment-wide, 5000/day is 62 such checks for everyone together".

**The direct transport spends none of that bucket and carries its own cost, in its own terms.** It does not put the raw key in the workspace: `renderWorkspaceAgentEnvironmentProfile` writes `export AI_GATEWAY_API_KEY='AI_GATEWAY_API_KEY'` — the placeholder name — and Plue's egress proxy holds the value and substitutes it (`FINAL.md:150-166`). What remains is spend, not theft: any process in a tenant's sandbox can send `Authorization: Bearer AI_GATEWAY_API_KEY` to `ai-gateway.vercel.sh` and have the proxy pay for it, for that sandbox's lifetime, which is the same exposure the Anthropic, OpenAI and Cerebras platform credentials already carry through the same proxy (`FINAL.md:168-173`). So the two costs are of different kinds: the relay spends a documented daily ceiling shared with signed-out humans, and the workspace key adds one more tenant-spendable credential, bounded by sandbox lifetime rather than by a ceiling, to a proxy that already holds four. That source's own closing re-comparison says the relay "is genuinely *better* on one axis … it bounds what a tenant can spend at a documented ceiling instead of at the sandbox's lifetime", and that the workspace key "still wins, by a narrower margin, on cost rather than on shape" (`FINAL.md:258-265`). The full re-costing, including the state-size tail and the missing authentication seam, is §2 "The two designs, costed". This ledger states both costs and takes neither side; the decision is the release owner's.

**Production today is not affected**, and this is checked rather than assumed: the pinned host's own source has neither binding — both greps return 0 at `f6b42949c705` (`[D25]`).

**How the key reaches a host is the release owner's decision and it is outstanding.** L101 recommends the key in the workspace, bound at Plue's existing egress proxy; R101 refuted the reason L101 gave for that recommendation while leaving the recommendation itself standing. `.artifacts/mvp-evaluator-reach-20260918/FINAL.md` is now **revision 2, written after R101**: its first line reads "**Revision 2, after R101.** The verdict is unchanged; the *reason* given for it in revision 1 was false", its §0 at line 44 ("What R101 refuted, and what replaced it") tables each refuted v1 claim — the first row marks the 8-questions-versus-64-states comparison **False** — and revision 1 is preserved byte-for-byte beside it at `FINAL.v1.md`. Read revision 2, not revision 1. This ledger records the decision as open and takes no side. Until it is made, the round-3 and round-4 host-flow fixes above stay off production.

### Canary walk run 3 — NOT_READY

`.artifacts/mvp-claude-orchestration-20260917/AUDIT-canary-walk-20260917.md` (phases A–D, reports `A3-*`, `B-REPORT.md`, `C-REPORT.md`, `D-REPORT.md`) opens **"Verdict: NOT_READY (REJECT)"** and enumerates six failing scenarios: land, activation of four of five jobs, apply refused with its own precondition satisfied, generic triggers, real GitHub event delivery (not executed), and the app failing to boot mid-walk. Its defect tables label six rows a release blocker — H-1, H-2, H-3, H-4, H-8 and A-1 (`grep -c "release blocker" "$A"` returns `6`, with `$A` as at `[D16]`). Not one of the five jobs completed its path, and `main` on the fixture repository never moved.

Against run 2, the audit's six-row blocker table closes one (pause integrity, §4 row 5) and leaves five open; its run-2 defect list is 24 rows, of which nine close outright and three in part — the four counts are the four commands at `[D16]`. The nine are B-1, B-2, B-4, B-8, B-9, C-1, C-2a, C-2b, D-1, and C-1 reads `CLOSED as written, replaced by C3-N2`, so that one close carries a successor defect; the three partial are A-1 for `failed` requests only, B-5 on the issues card only, and D-7's first half. The tenth full close, pause integrity, is the blocker table's, not that list's. The audit's own one action is "make the landing prepare exactly the change the job checked" (§8); `d5e1d8093491` is that fix, landed and, per the section above, not deployed.

### Canary W1 — the web half, verified live

`.artifacts/mvp-canary-walk-20260917/W1-REPORT.md`, run window 2026-09-18 23:21Z–23:50Z against web `42b8abbc1cf63065869ac26d7e0b7480cd50f4d1` read on both hosts. **This is the newest production evidence in this ledger.** Its table carries fourteen verdict rows (1, 1b, 2, 3a, 3b, 3c, 3d, 4a, 4b, 4c, 4d, 4e, 5, 6) and they split nine VERIFIED outright, one VERIFIED-and-OPEN (row 6, `VERIFIED (new request id + state), OPEN (the sentence)`), two OPEN (1b, 3b) and two PARTIAL (3c, 3d) — the five counts are the five commands at `[D14]`. W1's own verdict line says "Four of the six items pass outright; two pass in part", counting its six numbered items rather than its rows; that summary is not derivable from the table, so this ledger states the rows instead. W1's own "Still broken on the web half" list numbers **seven** defects: the heading is `W1-REPORT.md:37` and the seven numbered items are `:39-45`, both at `[D15]`. This ledger states **eight**, because it also counts the `/flow.create` refusal that names an internal flow id, which row 3c records as PARTIAL and that list does not number. Verified: the five job buttons mount in every conversation; a chat question beside an open setup is answered from that draft; the register preview states its limits and renders the cron verbatim while `--tokens 500000` and an unbounded flow are both refused with the range; `Discard draft` asks before it destroys; a settled failure retries as a new request. Partial: `/flow.create` refuses in the transcript but names an internal flow id, and signed-out `/issues.list` still opens its form beside the sign-in prompt. Open: `/chat.clear --summarize` is swallowed with no refusal at all; slash-line arguments land in the wrong field or are dropped; two person-visible sentences still leak internal codes (`Missing key at ["args"]`, `failed — invalid_receipt: …`); the pause refusal renders twice; a configured job's button does not always read its state; one composer submission was dropped once, unreproduced. The first two groups are lanes L102 (`L102-slash-line-arguments.prompt.md`) and L103 (`L103-sentences-and-card-state.prompt.md`).

W1 made production mutations and they are its own record: one `/chat.clear`, one issues Retry (`POST /api/repository-setup/trial 202`, request `bade9cbe-34aa-42d4-b7d5-cb85423dd5c3`, terminal `failed — invalid_receipt`), two no-op `trigger-pause` calls, one prepare and one approve whose registrar run failed, and a feature-draft edit discarded through its own confirmation. No GitHub write.

### Open gates, each with its owner

| Gate | State | Owner | Evidence |
| --- | --- | --- | --- |
| `main` does not typecheck | **Was open at the 03:14Z stamp; not measured as closed here.** The five errors L105's receipt recorded are gone from their source positions at the stamped sha (`[D17]`): `AppServices.ts` no longer exists under `apps/app/src`, neither harness mentions `socketUrl`, and `presentation.ts` no longer writes a card of kind `"experimental"`. Two commits in `81bca45092e9..0068f10c2b35` say they are the removal, also at `[D17]`. **Whether `pnpm run check` in `apps/app` now exits 0 is not measured in this section — this lane ran no build**, so the gate is neither asserted open nor declared closed; the owner closes it with one run | Lane L105 | `app-landing-20260919T0140Z.json` `mainIsRed` and `verification["pnpm run check (apps/app, combined tree)"]` — `exit 2 — five errors, ALL of them main@origin's own`, measured against `e35dcbfcfa17`, which is the last build measurement any receipt in this window holds. It no longer carries forward: `jj --ignore-working-copy log --no-graph -r 'e35dcbfcfa17..0068f10c2b35 & files("apps/app")' -T 'commit_id.short(12) ++ "\n"' \| wc -l` returns `8`, so `apps/app` has moved eight commits since it was measured |
| Third coding-host repin | Cut and stopped on the evaluator decision; 18 `flows/` commits wait behind it (`[D7]`), 16 of which change `flows/` source outside `flows/test/` (`[D8]`), and of those `211f4fde1e2b`, `d5e1d8093491` and `31aa4863f92a` answer walk run 3's blockers | Release owner (the design decision), then the release driver | `.artifacts/mvp-evaluator-reach-20260918/FINAL.md`, **revision 2, after R101** — §1 for the gap, §2 for the re-costed comparison, §0 for what revision 1 got wrong; `R101-review-L101.md` §3 for the refutation itself; `apps/server/src/recommend.ts:124,126` and `apps/server/src/jevRelay.ts:213-216` for the relay's per-request ration spend, both pasted at `[D21]`; the landing receipts above, including `app-landing-20260918T2305Z.json` |
| Canary walk run 4 | Not started. Run 3's blockers are fixed in source, not in the deployed host, so a run-4 walk before the repin re-measures the same host | Release owner | `AUDIT-canary-walk-20260917.md` §8; `EXPECTED.json` `run3` |
| Sandbox fleet size | **The guard that held it is gone and nothing committed replaces it.** At the 03:14Z stamp a local `bash capacity-guard.sh` loop, pid 77296, held the fleet at 2 live replicas for four hours from 2026-09-18 22:59:56Z. That process no longer exists (`[D18]`), and the window it announced ended before this stamp. What the fleet is now was not measured: this lane makes no cluster request. What is committed is unchanged — `sandbox_worker_nodes` is `default = 12` at `c0bd4c8218c3` and `terraform.tfvars` carries no pin (`cd ~/plue && jj --ignore-working-copy file show -r c0bd4c8218c3 infra/terraform/environments/prod/terraform.tfvars \| grep sandbox_worker_nodes`, exit 1) | Release owner (land the tfvars pin) or Will (raise the four GCE quotas the variable's own doc names) | `infra/terraform/environments/prod/variables.tf:407-425` at `c0bd4c8218c3` in `~/plue`, whose doc comment names the four GCE quotas each node spends; `[D18]` for the guard |
| Plue alert remediation | **Was left disabled by the repin-2 deploy at 08:45Z on 2026-09-18; no later change is recorded.** No lane read the flag at this section's stamp, so this is a deploy-time fact, not a present-tense one. The driver prints the exact re-run: `--skip-docker --tag c0bd4c8218c3 --deployment-state active --activate-alert-remediation` | Release owner | Driver log `~/.config/smithers-autodeploy/logs/plue-20260918T075227.786738Z.log:8155`, byte-equal; that run is the last Plue deploy at `[D4]`, so nothing later has changed the flag |
| Alpha users' identities | Not supplied. Three gates admit a user on different keys: the Smithers allowlist by login, the WorkOS waitlist, and Plue's closed-alpha whitelist by username **and** GitHub-verified email. `auth.closed_alpha_enabled` defaults to `true` (`internal/config/config.go:589` at `c0bd4c8218c3`) and the whitelist skips any unverified address (`internal/services/auth.go:623`, `if !email.Verified { continue }`) | Will | `HANDOFF-claude-20260917T0645Z.md:136` |
| Bounded checkpoint projection (L97b) | Deferred. R97c REJECT B1: a settled run's trace card is `isRuntimeOwnedCard`, so retention never evicts it and the run's control journal is pinned with it — 100 settled runs at the shipped 8 MiB budget leave a 13.73 MiB checkpoint booting in 1,647 ms, growing linearly with the run count. Those three figures are quoted, not re-derived here. The three candidate commits `66244a19ce42`, `0a6efd139059` and `1cce989ae071` resolve but none is an ancestor of the stamped sha (`[D22]`) | App owner; unassigned at the stamp | `sed -n '12p;55p' .artifacts/mvp-claude-orchestration-20260917/R97c-review-L97b.md` returns the verdict line and the measured row `[P9] dispatched=100x128KiB run traces retained=100 checkpoint=13.73MiB budget=8MiB boot=1647ms valid=true reopenedTraces=100` |

The walk's own app-boot blocker (A-1, `The app-event-checkpoints store exceeds the 67108864-byte load budget`) is a different budget from R97c's 8 MiB retention budget; `50479793be89` (L97) landed for the first and **is in the deployed build** — it is a web commit (`app-landing-20260918T1512Z.json` `half` = `WEB …`, zero `flows/` paths) and an ancestor of the stamped sha (`[D20]`), which is the sha the driver last deployed at `[D3]` — while the second is the row above and is not fixed. Do not read either as closing the other.

## Deployed release as of 2026-09-17 03:40 UTC (superseded)

| Component | Verified identity and result |
| --- | --- |
| Web | Root deployed reviewed Smithers `4ca45c0b2d2e52ba0e7d59db4c2ce73b1dbf5c42` through the clean sanctioned driver at 03:24:33.784 UTC on September 17. Cloudflare `04db10ed-afd4-4961-96d0-8d742618a25a`; build 03:23:55.864 UTC. Driver exit 0 and both actual build/HTML probes pass. Includes practice file-source resolution and durable guide admission. |
| API | Two actual ready pods report Plue `7ca92e2309e3`, created at 03:21:03 and 03:22:32 UTC. Source is independently reviewed; the concurrent owner's deployment driver receipt remains requested. Prior owned API `50410f8e` passed Helm revision 399. |
| API image | Current pods: `sha256:487335a7de388306afd3d64501686c81c4bb7b06f72939139ba31cecd172253d`. Previous observed `d858fcd8`: `sha256:f21f2ba58c5c046fcf4d70b35db32d84a7968e07ce9a7583521fcdba92466c12`. |
| Native coding host | Last measured bound workspace artifact: source Smithers `445fb627a82096285759badd8beace6048d107c7`; SHA-256 `2541b86b149582ed309a57dc5a324f4870740bae369abe9cf097192cb1b8dbd0`. No workspace repin is claimed from the newer API observation. |
| Database | Applied repository jobs, comments and capability-binding migrations through `20260916233000`: 17 statements, 7.79 seconds. Atlas status: 115 executed, zero pending. |

The owned sanctioned deploy drivers preserved Worker/DO identities and secrets, enforced the API deployment lock and clean main source, and verified their replacement API pods. The latest concurrent API deployment is distinguished above. Existing workspaces are never silently repinned to the new native host.

## Passing evidence

- Agent library: 731 tests pass, with 100% statements, branches, functions and lines; typecheck and lint pass. The final human-wait tests cover retained questions, real wait/resume races and actual terminal completion.
- Worker typecheck and Effect I/O policy pass. The clean site build renders 274 pages with all 855 required URLs. Actual deployed site probes pass 415/415 on each domain, including all 44 previously failing legacy documentation routes.
- Actual public browser checks on deployed `99f932d1` pass: all five setup entries, signed-out setup gating with zero setup requests, prompt edits across reload, one README search row and one bound read action, 320px drawer Escape/selection behavior, and no overflow or browser exceptions.
- Actual fresh and reused file forms on both domains focus Path after the new request projects, accept unassisted keyboard input and return real file responses. Two-tab ownership handoff preserves file cards through reload. Practice-profile upgrade preserves exact Full diff routing and completed research without a repeated tutorial request.
- Actual local native execution covers all five jobs, independent reproduction, manual work and author replies. An actual Cerebras first-run test begins with empty eval cases, authors source-based cases, executes issue investigations, uses independent held-out judgment, runs a second trial job and applies the result. Its repository API is an explicit local fixture; this does not establish deployed setup or model quality across repositories.

The [frontend record](verification.md) preserves the broad suite result: 4,301 passed, seven opt-in host cases skipped, and three unchanged SearchSeam timeout failures; the isolated recheck passed 21/21. The broad result is not relabeled as a clean run. [Application captures](README.md) use explicitly fixture-backed responses and remain distinct from production evidence.

## Production findings being resolved

The saved test account `codeplanesmithers` is allowlisted and an administrator. It is not evidence of ordinary-account eligibility. Real PostgreSQL tests separately exercise the free one-workspace billing policy.

On `codeplanesmithers/canary-sandbox` (repository 3503), `/issues.setup` returned in 66 ms, persisted request `1a223620-412b-4055-bfa7-aa4c6b6f1eca`, acknowledged HTTP 202, and left Chat editable. The first real inspection then exposed a provider-proven missing VM in an old suspended primary workspace. No native job, binding or registration was activated. The failure remained visible and reconnectable. Backend recovery is implemented and verified with actual PostgreSQL tests before retrying the same durable request. The free one-slot cases include missing primaries in running, starting and suspended states, temporary provider failure, changed VM/generation, an existing binding, and a registration arriving during the probe.

The original setup request reconnected on API `bb26a6b1` with the same identity and HTTP 202. Chat remained editable and the command returned in 23 ms. That run reached an existing gateway resume, whose ordinary startup response was incorrectly surfaced as a failed setup. The matching typed pending repair keeps unverified candidates unbound until compatibility is proved; generic conflicts and transport failures remain failures. Worker main `64d4f4edc9b4d10dea9256f0e4a8f44d4806007c` has this repair, with 16 setup tests and typecheck/Effect policy passing; both it and matching API `81c3e741` are deployed. The API passed actual PostgreSQL pending/compatible/incompatible selection, billing and registration-race tests before deployment.

The original inspection completed as `run-1` while the browser was closed, on compatible workspace `f387bca6-6c93-4e22-9a5a-dbe0b455d51d`. It inspected README plus native/GitHub issue and GitHub PR history at exact source `3f93dd583d9608c833483b7c7d1578cfee2b3ddf`, and generated two editable held-out cases. No manual quota change, deletion or workspace repinning was required.

A later browser process opened inspection `88f7ff00-eee0-48b9-b8aa-d7879a1c4a40` (`run-2`), which generated one required case from actual issue 53 and README. Evaluation `0437e991-152f-42f8-8060-987556758f2c` (`run-3`) completed in 3 minutes 26 seconds at 23:30:37 UTC. Candidate revision 2, digest `c81817b6e662d782271e969e342034e783e5023c022b4c7442b2e7d233de86d0`, passed its required case: research, duplicate search and reproduction completed, and the independent judge cited their actual executions and repository evidence. There were no public actions or pending approvals. The direct persisted receipt and native run summary establish this result. The first browser harness incorrectly timed out because its passive response listener lost terminal response bodies; that failed harness record remains separate from the successful execution.

The saved browser profile also reopened with older application state and no setup card. Its page and worker saw an empty OPFS directory; cached database handles returned NotFound, while an existing sync handle remained writable. Chromium held its quota database in a `WebStorage-doomed-*` directory. An isolated persistent-browser experiment reproduced that signature after browser-level filesystem clearing. This establishes the unlinked-file mechanism, but neither its original trigger nor an actor. No production storage clear/reset was performed to diagnose it, and no speculative storage patch was made. Four controller/persistence probes and a separate isolated full-process restart passed. Only whitelisted recovery metadata was saved; no raw recovery export or credentials were written to artifacts.

Root acquired the same atomic profile lease used by the real E2E suite and retained it across normal browser close/relaunch. The resulting profile had discoverable SQLite/WAL/journal files and a normal quota database. Fresh `/issues.setup` returned in 294 ms, with Chat editable, and inspection `eb298f77-af74-4584-b4bb-5f7d52a0038c` completed as `run-5`. It inspected source `3da2c1ef956fa5af4902410cc7a495aeda8564b8` and generated two repository-based cases. A full browser restart, without a final recovery-export flush, restored the same stream `c8b7e367-d7b4-4244-9b06-3a4d47266fb1`, completed request, revision 2, cases and workspace. Neither restart nor repeating `/issues.setup` issued another inspection POST. This closes controlled inspection persistence on actual production. It does not recover the earlier unlinked store or prove cross-browser setup recovery; the server's request lookup still requires a locally held request ID.

The completed inspection was incorrectly archived as running when generated cases advanced the draft. This confirmed receipt-ordering bug is repaired in `d8eec1a6`, included in the deployed release. It archives the authoritative terminal receipt and reconciles only exact matching historical identities.

The actual trial was edited through the UI into a README-based documentation question. Candidate revision 4 / digest `b72075b277a98123ed63a1586406cb1001ab8aebf527de41f3e0138d9cd3fae1` retained both generated cases unchanged. Evaluation `7ad291a2-4fce-4feb-ae9b-cba34b7e3ca5` / `run-6` passed both `readme-purpose-question` and `issue-53-purpose-section` in about four minutes, with source evidence and independent judgment. Click returned in 76 ms; Chat remained editable. A second full browser-process restart under the same lease, without a final export flush, restored the exact candidate, two passed cases, workspace and stream with zero setup POSTs. The observed checkpoint reason was compaction; no retirement was recorded.

| Production operation | Actual request and execution | Result |
| --- | --- | --- |
| Scoped issue trial | `c74b5f6f-e096-418e-8d92-90e5f14b53ad`, setup `run-7`, native job `run-8` | Created native issue 54 and scoped registration `3c3d88b6-02e2-4161-b5a1-2ae9b1c8c981`; actual native outbox dispatch completed at source `e1846f176a5d42acfdf5e37ee7c36339f182a1fd`. |
| Enable | `81064291-f23e-4adc-9117-c1895307dd64`, `run-9` | Actual registration `a5622c49-d022-4ef9-9f56-d5980ea6cc16` enabled for the exact revision/digest/source; independent backend GET confirms it and disables the trial registration. |
| Manual Research | `0cf5bf47-8e5d-4338-a532-797ad866db80`, setup `run-10`, job `run-11` | GitHub issue 1 researched under the applied policy; exact manual dispatch, registration, candidate and execution evidence returned. No public reply or source change requested. |
| Pause | `6c53df36-9ac8-4d37-aa07-45952a4c5837`, `run-12` | Completed on deployed `7f18dfb8`; independent backend GET confirms both registrations disabled. The card shows Off and advances the draft so reactivation needs new reviewed evidence. |

The trial exposed a transient completion handoff: its completed run projection did not yet contain a valid typed result, and the UI stopped polling with an error. Existing Reconnect recovered the same request without a second dispatch. The three-file repair `7f18dfb8` keeps only absent final output pending for a persisted 60-second interval, preserves the run, refuses malformed or mismatched output, and allows a later valid result to reconnect even after expiry. Both new absent-output tests failed on the baseline; all 23 setup tests and the actual Worker typecheck/Effect policy pass. The separate successful native job `run-8` summary omits finalOutput entirely; its setup flow independently verifies typed native ancestry. A completed status alone is not accepted as a job result.

The trial proves actual native issue-event dispatch. Manual GitHub research is explicitly a manual delivery and does not establish signed GitHub webhook ingestion. No active automation remains on this disposable fixture after pause.

A separate real-browser test held the public repository catalog's actual HTTP 200 response. A command entered while it was unresolved incorrectly asked for sign-in and did not resume when the catalog arrived. The readiness repair in `50ee5df1` passed 12/12 checks on both domains. A subsequent network-failure retry exposed a second path; its repair is deployed in `0f9a0976`. All four actual fault→fresh catalog 200→README 200 scenarios pass across apex/canary and direct/reload variants: 58 checks, zero browser exceptions, one real README read per scenario, honest pending/error state, editable Chat and toast completion only after the read. The catalog follow-up is closed.

## Source retention follow-up

Smithers main `445fb627a82096285759badd8beace6048d107c7` adds exact verified PR/push source retention and native import. Typecheck passes; ten focused tests pass without skips. Authenticated HTTP Git tests import previously absent base and candidate commits, then prove ancestry, export, diff and command checks while preserving the editor revision, unsaved bytes and bookmarks. Replay does not repeat retention. A source-only native host test verifies the additional `repository-source/v1` capability.

The clean bundle's SHA-256 is `2541b86b149582ed309a57dc5a324f4870740bae369abe9cf097192cb1b8dbd0`; Node and Bun entry points pass. Its immutable artifact is uploaded and pinned by Plue main `bb26a6b1582bd4900d63091be911eb1b512ea1c4`. The full PostgreSQL repository jobs suite passes in 3.137 seconds; the full native adapter suite passes in 57.086 seconds, both without skips. Source/capability/API, landing/routes and the complete error-registry checks pass. The error audit also registered eight existing landing codes, preserving their HTTP status and messages. No new migration was added. API `bb26a6b1582bd4900d63091be911eb1b512ea1c4` is deployed at Helm revision 393 with two verified replacement pods, image digest `sha256:33596ab0a01da4080acd438089bdbd5548e51b7ded6d84d15e566f979c038e73`. The live API coding-host hash matches `2541b86b`. Real cloud PR/publication proof remains pending.

Historical eval cases still require their exact source to be available locally. An absent or moved historical source fails before scoring; this release does not silently substitute the current head or alter expected answers to obtain a passing score.

An actual authenticated SSH measurement on the selected production VM used its advertised `ssh.jjhub.tech` endpoint and pinned host key. PR 2 head `561aa58f3a76abe79768c904d36f36cd0c32e41f` was not indexed by native jj; underlying Git object presence was not measured. Its base `26fd39728e45613e8ae9eab1a44c957d56556f47` was indexed. The probe recorded the editor revision, bookmarks and nine saved setup files before any canary import. No source import, PR run or landing is claimed from this measurement alone.

Actual cloud retention succeeded for those exact PR 2 head/base commits. Native review `run-4` then failed before model work with `source_unavailable: Source import could not complete; retry`. The failed receipt and source measurements remain preserved; CI and landing did not run. A protected, read-only VM diagnostic proved that direct HTTPS is refused by the VM firewall, while the sanctioned managed proxy and CA plus the exact cached credential return a valid Git advertisement with both retained refs matching. The adapter's isolated Git environment had discarded mandatory proxy/trust settings.

Plue main `06f4c9032e9b0fd56844e764389a132fc3ea087c` contains ten independently reviewed files. The native repair reads only the root-provisioned route and trust bundle, supplies explicit Git proxy/CA/verification settings, retains disabled redirects and ignores caller-provided proxy/configuration. Eleven source-import tests pass with zero skips, including an actual HTTPS CONNECT/TLS/cache/native JJ import and rejection of an untrusted certificate before credentials or mutation. The editor revision, bookmarks and unsaved files remain unchanged. An adapter-only managed refresh lets healthy bound workspaces receive this Python update without changing their host artifact, source or reporter credentials. Expected installed adapter SHA-256 is `30e24973f73cb1655d4f702bf334345f96011a4ddd41315bff4421de016bc074`; actual production installation remains to be measured after API rollout.

The same candidate adds the existing idle-cleanup gateway exclusion to the last-terminal suspension CAS. Seven actual PostgreSQL/service scenarios pass with zero skips; a starting/running bound gateway keeps its VM alive after terminal close. Runtime/refresh checks pass 38 top-level tests with five existing platform/database skips. Its inherited caller-scoped mirror credential change passed independent source review and 23 selected tests with zero selected skips, including actual local Git and git-sync HTTP transport. The git-sync binary identifies itself as a development build, so the result claims executable transport proof, not authenticated binary provenance. The separate pre-existing crash/provider failure after the suspension CAS remains a documented reconciliation gap. The exact clean API image is deployed at Helm 397; actual live coding-host SHA remains unchanged and verified.

After rollout, ordinary Worker provisioning reused its cached gateway. A read-only SSH diagnostic measured the old installed adapter `ef2c7be588c97042bf6c94a436f6b4684a9809005b7ab53303ea8f421b8a67e9`. Direct authenticated backend renewal with the supported `repository-jobs/v1` requirement then returned `409 coding_host_unavailable: workspace native runtime could not be verified; retry`. Two observations agree; no new PR job was launched. An earlier operator probe used the unsupported `repository-source/v1` request value and correctly received 400; that is harness misuse, not a product defect.

The protected read-only VM diagnostic isolated the refresh failure: the doubly encoded command exceeded Linux's single-argument limit. A harmless 134,210-byte argument returned `E2BIG`, while a short argument and isolated Python passed. All protected workspace/configuration identity fields matched, and the maintenance directory was absent: Python had never started. The repair removes only the redundant outer encoding. An actual Linux full-provider-command regression proves the old 134,274-byte invocation fails before maintenance and the corrected 102,802-byte invocation returns `updated`, then `unchanged`, with exact adapter hash `30e24973`. It preserves the old open inode, configuration, credentials, reporter, user files and both evidence stages. Eight tests plus 27 subtests pass without skips; the source is independently reviewed.

Plue `50410f8e40b8d310cc300e3673f081e5a70c6818` combines that three-file repair with six reviewed mirror files. The mirror excludes `refs/smithers/`, `refs/jj/` and `refs/pull/` consistently from transfer, pruning, verification and retry. Actual authenticated local Git HTTP/git-sync tests prove protected refs remain unchanged and private source objects are absent, while user notes/custom refs still transfer and ordinary obsolete refs prune. Twenty-five tests plus six subtests pass with no skips. This is executable transport evidence, not authenticated git-sync binary provenance or a production mirror operation. The exact nine-file source is pushed; its API deployment and actual bound-adapter refresh remain open.

The `50410f8e` deployment subsequently passed at Helm 399. Supported normal gateway renewal returned HTTP 200 / `running` on the same workspace and gateway. The protected read-only SSH diagnostic measured the exact new installed adapter `30e24973f73cb1655d4f702bf334345f96011a4ddd41315bff4421de016bc074`; its source/import capabilities, native editor revision and saved setup remained present. The configured proxy/trust/cache path returned a valid Git advertisement for the retained PR head and base. This closes managed refresh on the actual bound VM without a manual installation or host repin. A fresh `release-20260916-r3` source canary starts from new measurements; old failed attempts are preserved.

The latest web polish passed independent review and focused checks: card 29 tests plus application check, approvals 166 focused tests plus final 21 and application check, and recommendation 87 tests plus application check. Recommendation coverage includes retained account/origin cooldown, held response bodies, duplicate in-flight work and pending-outcome ownership. An earlier actual public README 502 recovered on the next read and subsequent checks; its cause remains unknown and is not attributed to a redeploy.

Actual approvals on Worker `712408a5` used real production responses with two controlled browser-side holds. Submission returned in 118 ms; Chat accepted input while provisioning and the approvals read were independently unresolved. Duplicate input caused one provision and one read. The loading toast remained through both stages, showed no premature success, and resolved only after the real inbox response. A separate deliberately overlong hold reached the actual 30-second timeout and showed a retryable failure. The first exact-text acknowledgment probe timed out because that transient text was not present when queried; that failed observation remains recorded alongside the successful behavioral proof.

Concurrent releases changed the served Worker through `51ac0bd5`, `6f600390` and `aa9f403e` before the owned `0f9a0976` deployment; exact-build guards paused dependent actions for review. Review found that sleeping responses could bypass the resume floor and renewal could discard required-capability validation. The four-file repair `4bf770b0411c83d378bf1c8975a0b551e54f164c` passed 126 tests/881 assertions and the actual Worker typecheck/Effect policy. Two deployment builds failed before publication on the same unchanged Starlight module's 60-second Vite transport timeout under shared-machine contention. The later clean `7f18dfb8` build completed normally, and its verified deployment includes this repair plus history and completion handoff. Both earlier failed builds remain recorded.

## Exact-source PR checks and blocked feature landing

Fresh attempt `release-20260916-r3` passed native PR review `run-13` (request `03e07968-fb7c-4362-b632-01c91650273d`) and CI `run-14` (request `3b4b8fd2-5dc3-4c82-835d-40f7baa33112`). Both name GitHub PR2 head `561aa58f3a76abe79768c904d36f36cd0c32e41f` and base `26fd39728e45613e8ae9eab1a44c957d56556f47`. The actual review and README command passed. Native import indexed the previously missing head; exact source retention succeeded, with unchanged editor, bookmarks and all 18 saved setup assets. The Worker stayed `712408a5` through these operations; API rollout to `d858fcd8` overlapped the CI interval, so this is not an API-5041-only claim. The bound host and installed adapter stayed unchanged.

Feature `run-15` received the inspected approval for exactly `.canary-releases/release-20260916-r3.txt`. It created native source `255b75855d964660d7c55ee39e96ba3d6bc7977c`, parent `e1846f176a5d42acfdf5e37ee7c36339f182a1fd`; the required marker-byte command and independent AI scope check passed on that actual source. Delivery then failed at append preparation with HTTP409. The typed job result is **partial**, not landed, and cloud main remains `fb8c7b08f0d0feb5887cabbec5650e28dae2e6c1`. No new landing request was created or queued. The original canary helper retains its last pre-approval checkpoint because it correctly refused to certify the partial result; native terminal receipts are authoritative.

Read-only immutable exports settle the cause. Source base `e1846f17` has tree `f1942320c26f1060a2a25cc8d06a2eaafa477776`; main `fb8c7b08` has tree `cedfcea409ddbdf2029e862266656255228b21c5`. Their only differences are the 18 generated workflow/prompt/eval files under `.smithers/repository-jobs` and `.smithers/flows/repository-jobs`; every other path has identical digest. The native append contract requires a base whose tree matches main and correctly refuses this one. Setup materialization snapshots these files into the workspace before live work. Live source selection must preserve that saved configuration while checking exactly the candidate to be landed. Weakening the native gate, discarding configuration or landing the unreviewed setup files is not a repair. The approved marker and all configuration remain retained.

## Web recovery and guidance follow-through

Recovery `87be163c` shipped in `54b4f47a`. Its 22 independently reviewed files passed 137 focused setup checks, 296 adapter checks, 23 shared-schema checks, seven actual SQLite journal/crash checks, and application/server checks. Current registration is independent of candidate receipts; missing local state cannot imply Off or replay old work. Recovered observers cannot renew or provision a workspace, and server-derived ownership prevents cross-account adoption.

Actual authenticated discovery returned the two-case revision-4 candidate and old completed apply `run-9`, independently of the paused registration. Holding the real discovery response through reload produced one GET, zero setup/provision/workflow mutations, editable Chat and no Off claim while pending, then Paused. A full browser-process restart under the unchanged profile lease subsequently preserved local stream `6e546911-02dc-41cf-8116-8239d5c5790c`, revision 5/digest `0cca1252856dc272a89ae5a40774e96220b81a4ef42eea5e024ba7397afec67f`, both cases, workspace and inspect/eval/trial history with one GET and zero extra work. The site changed from `54b4f47a` to the independently reviewed external `624261df` during that restart; it is explicitly a version-transition observation. An immediately clicked view did not persist, so view-click durability is not claimed from this run.

Genuine absent-card recovery passed on `884b915d`: the canary origin signed in normally through the existing GitHub popup and had zero persisted fixture setup cards. Two identical `/issues.setup` commands returned in 18 ms, shared one actual GET/HTTP200, and made zero setup/provision/workflow mutations. Chat remained editable. The recovered card retained the exact two-case revision-5 draft and workspace, displayed Paused, and observed old apply `run-9` without inventing eval or trial authority. The profile was neither cleared nor copied. Private exports remained only in memory; artifacts contain selected evidence.

Discovery separately displaced completed pause `6c53df36`/`run-12` with the old apply receipt without archiving it. Native run-12 still exists. The four-file repair `bf3ad42c` is reviewed and pushed: it preserves the displaced exact receipt using existing identity, terminal preference and 50-entry history rules. Its real red baseline, 63 focused checks, 28 RPC checks and app check pass. It is not in deployed `4ca45c0b`; no private-state reconstruction was performed.

The real-provider guide has two distinct repairs and a remaining quality failure. On `54b4f47a`, run `7c67641a` sent a JSON guide argument to a bare-ID parser, received a refusal, then falsely claimed to have read configuration. The three-file `884b915d` repair accepts the strict JSON alias, retains the canonical bare argument and clarifies the instruction; 44 focused checks, 17 instruction-budget checks and app check pass. Actual run `ab754f93` on that release read the exact candidate and completed both journal legs, but still asked which labels to automatically apply. This is unsupported: the label setting filters incoming issues.

A fresh canary conversation confirms quality remains open independently of prior chat. Configure returned in 38 ms and inspection `baedb3b1`/`run-16` completed at 03:25:59.666 UTC after about nine minutes from request, retaining the same draft and reading actual repository history at source `255b7585`. Guide `dbdf21c0` then read the exact candidate and completed two persisted HTTP legs, but asked which fixed category Research issue should classify new issues as. Root's semantic review fails this confusing question: classification is inferred for each issue, while setup should clarify consequential editable choices. The client loaded `884b915d`; Worker `4ca45c0b` and API `7ca92e23` deployed during inspection. This is not a single-version guide-quality pass.

The separate immediate-boot defect was a lost in-memory guidance request after optimistic HTTP admission rolled back. Deployed `4ca45c0b` now persists a fixed guide request until actual owned HTTP admission. Committed-only lookup prevents re-POST after restart or card acknowledgment failure; one bounded automatic persistence retry precedes a visible, explicit retry. An admitted turn with a failed card save reports an outcome-save error, never “didn't run.” Local evidence: 131 focused, 25 RPC, 27 ordinary-chat, 46 affected and 15 practice-file checks plus app check. Actual new-version held/restart guide proof and useful conversational follow-through remain required.

Practice recommendations passed actual anonymous tests on both `54b4f47a` hosts: `repo: null`, 166 commands, HTTP200 and real provider recommendation receipts, editable Chat and zero browser exceptions. Cold public commands subsequently passed 66 actual normal/held-catalog/reload checks on `624261df`; the same held/reload scenarios passed 34 checks on `884b915d`, plus ten persistent-browser checks. Actual catalog and README responses, duplicate coalescing, toast completion through held execution, Chat, narrow layout and restored files passed. These close the corresponding cold-command reload defect. Deployed `4ca45c0b` includes reviewed practice file-source repair `b9e3ac68`. Both actual hosts pass 12 of 13 practice checks: bundled direct/directory/form reads, persisted files through Start Here, 390px layout and editable Chat. The remaining failure is focus lost when an asynchronously populated path field changes from INPUT to SELECT. Bare commit/issue/PR practice source resolution and the first-run target/identity race remain separate observed defects; the full practice canary correctly remains failing.

## Public catalog request ownership

Intermittent failures include canary README502 at 02:00:17 UTC and catalog500 at 02:02:52 and 02:49:23 UTC. A correlated catalog failure on `884b915d` at 03:07:05 UTC, ray `a3c4e60f5f322271-SJC`, carries the Worker runtime's hung-request cancellation. Actual local workerd reproduces it using the unchanged catalog handler: the first request owns held upstream I/O, while a second waits on its module-global semaphore and receives 500. A request-owned control returns 200/200. The shared ManagedRuntime is not needed to reproduce this defect.

Root accepted the two-file candidate `7e496e55`, landed and pushed as `ff1c6dd5`: concurrent misses own their bounded I/O and return their own completed snapshot; completed isolate/edge cache data and existing TTL/roster/credential rules remain shared. Actual fixed workerd returns cold 200/200 and warm 200 without another upstream read; 21 focused, 70 adjacent checks and server check pass. Concurrent cold misses may duplicate metadata reads. Deployment and live validation remain open. The App-token and public-activity gates independently reproduce cold 200/500 in actual workerd, with request-owned controls passing 200/200; their separate repair is underway. The README502 is not closed by these results.

## Native source and CI follow-ups

The reviewed native creation primitive is pushed as Plue `79132069` and Smithers `161ecf8e`. It makes one immutable child of exact main without moving the editor, changing checkout files, rewriting descendants or moving bookmarks. Its operation receipt binds owner, request, base and exact bytes; verification reconstructs the request from native trees before publication. Actual native tests cover creation, preservation, replay, tampered/copied receipt refusal, races and transfer. Host composition tests additionally prove fresh checks and retained source after later failure. A lost creation response now retains the actual prepared request ID through retry exhaustion. These are source/local proofs, not deployed landing proof.

Two required follow-ups remain: installing the reviewed compiled helper on both existing and fresh workspaces, and retaining/importing authoritative current main after a landing creates a new cloud commit absent locally. Python alone cannot advertise creation capability; old jobs must not be silently repinned. The retained partial feature run-15 is not made successful by a later canary.

AI checks now preserve nested execution errors and receive bounded exact-source helpers/conventions with an explicit availability manifest. Context `f4e46b21` passed 25 focused and four actual native/scripted-model tests before landing. Deleted files currently refuse as missing candidate context; a separate exact-base context repair is in progress, keeping old and new helper identities separate. Reviewed active CI rules are not yet reused by all dependent jobs, and the modern exact-source check receipt still needs its native landing bridge. Pausing CI stops automatic launches; required reviewed protections should remain until a reviewed policy edit changes or removes them. Existing external-status and human-approval gates remain authoritative.

## Gates still open

- Deploy compatible native creation/helper changes; prove two successive exact-source feature landings and independently advanced main while preserving saved setup and editor state. Retain and resolve the original partial attempt honestly.
- Finish deleted-file check context, reviewed CI-policy reuse and authoritative check-to-landing proof; measure real provider violation, clean and unavailable cases.
- Put the Checks tab's `Required before landing` option behind O-07's evidence. The setup guide's first CI question can no longer make a check required, but the tab still offers the policy with no eval or trial receipt. Activation already binds the evaluate and trial receipts to one candidate digest and revision, so flipping a check to required forces fresh evals and a fresh trial, which is the separate reviewed decision O-07 asks for.
- Make the setup guide ask useful supported questions; verify new-version durable admission through held recovery/restart, edits, prompt review, eval and trial.
- Deploy and verify pause-receipt archival and catalog request ownership. Finish observed practice source/default-target defects; retain unrelated public failures until their causes are measured.
- Wire and test the feature setting “Start from approved issues”: current activation registers issue events regardless of `connectIssues`. Preserve explicit manual work and scoped trials, and verify approval semantics instead of treating the checkbox as execution proof.
- Complete the remaining all-five setup/event/PR/feature/chore paths, actual scheduled delivery/pause/restart, ordinary non-admin onboarding, and retained-feature/flag/removal release audit.
- Keep existing Markdown relative-link/image and binary-content limitations explicit; direct file access works. The broader Markdown/revision architecture change remains deferred in the engineering plan.

Local receipts are retained under `.artifacts/mvp-release-20260916/` and `.artifacts/canary-tester-20260916/`; this tracked record carries their durable conclusions and exact release identities without copying authentication state or credentials.
