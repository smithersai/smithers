# @burns/config

Reserved workspace package for shared repository tooling configuration.

## Current status

This package is intentionally minimal and currently acts as a placeholder for future shared config such as:

- shared TypeScript presets
- shared ESLint presets
- shared formatting/linting conventions

At the moment, repository-level TypeScript defaults are defined in:

- `tsconfig.base.json` (repo root)
