---
title: JSX Installation
description: Install Smithers for the JSX workflow API.
---

<Tip>
Run `bunx smithers-orchestrator init` to scaffold a complete `.smithers/` project. The manual steps below are for standalone projects.
</Tip>

## Prerequisites

- Bun >= 1.3
- TypeScript >= 5

## Install

```bash
bun add smithers-orchestrator ai @ai-sdk/anthropic zod
bun add -d typescript @types/react @types/react-dom
```

## TypeScript Configuration

```json
{
  "compilerOptions": {
    "target": "ESNext",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "jsxImportSource": "smithers-orchestrator",
    "strict": true,
    "noEmit": true,
    "skipLibCheck": true
  }
}
```

`jsxImportSource` tells TypeScript to use Smithers' JSX runtime instead of a browser UI runtime.

## Project Shape

```txt
my-workflow/
  package.json
  tsconfig.json
  workflow.tsx
  main.ts
```

## Optional: MDX Prompt Files

```ts
// preload.ts
import { mdxPlugin } from "smithers-orchestrator/mdx-plugin";

mdxPlugin();
```

```toml
# bunfig.toml
preload = ["./preload.ts"]
```

```bash
bun add -d @types/mdx
```

## Verify

```bash
bun run main.ts
```

## Next Steps

- [JSX Quickstart](/api/quickstart) -- Build a two-step workflow.
- [Tutorial: Build a Workflow](/guides/tutorial-workflow) -- Schemas, agents, and output access.
- [MDX Prompts](/guides/mdx-prompts) -- `.mdx` files as prompt templates.
