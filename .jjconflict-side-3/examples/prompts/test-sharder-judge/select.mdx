export default function Prompt(props) {
  const { children, components, ...data } = props ?? {};
  return (
    <>
      {"Example prompt: test sharder judge select"}
      {"\n\n"}
      {JSON.stringify(data, null, 2)}
    </>
  );
}
