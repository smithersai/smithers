Scan the directory "{props.directory}" and discover:

Focus: {props.focus ?? "general code quality"}
File patterns: {props.glob ?? "**/*.ts,**/*.tsx"}

For each finding, categorize it and rate severity.
Return a structured discovery report.
