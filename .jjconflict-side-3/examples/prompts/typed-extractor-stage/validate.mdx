export default function Prompt(props) {
  const { children, components, ...data } = props ?? {};
  return (
    <>
      {"Example prompt: typed extractor stage validate"}
      {"\n\n"}
      {JSON.stringify(data, null, 2)}
    </>
  );
}
