Find all occurrences of this pattern in "{props.directory}":

Pattern: {props.pattern}
Refactoring: {props.refactoring}

Example: {typeof props.example === "string" ? props.example : "N/A"}

List every file and occurrence that needs changing.
